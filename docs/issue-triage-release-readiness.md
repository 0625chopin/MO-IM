# 열린 이슈 3분류와 v1.0 릴리스 종료 조건

- **일자**: 2026-07-30(27일차) / **담당**: CREW(팀장 배정)
- **왜 지금인가**: v0.1 로드맵 Task 001~045가 24일차에 전량 완료된 뒤, 열린 이슈 순감이 최근
  2회차 동안 0이었다(25일차 −5, 26일차 +5). Task가 남아 있지 않은 지금 "언제 끝나는가"에
  답할 유일한 수단이 **열린 이슈를 릴리스 관점에서 분류하는 것**이다.
- **단일 소스 원칙**: 이 문서는 이슈 **내용을 복제하지 않는다.** 각 이슈의 전문·재현·영향은
  `docs/ISSUES.md`가 단일 소스이고, 이 문서는 번호를 참조하며 **분류와 그 근거 한 줄만** 추가한다.
  갱신되는 집계값(건수)도 이 문서 안에서만 쓰고 다른 문서에 복제하지 않는다(CLAUDE.md 원칙).
- **기준 시점**: **이 문서의 모든 숫자는 2026-07-30(27일차) 회차 종료 시점 기준이다.** 아래
  §0 제외 목록의 처리 결과에 따라 다음 회차 갱신 시 숫자가 바뀐다 — "확정된 최종 집계"가 아니라
  "이 시점의 스냅샷"으로 읽는다.
- **모집단**: `docs/ISSUES.md`를 전수 추출한 결과 **상태가 "열림"으로 시작하는 이슈 46건**
  (I-016~026 절의 템플릿 항목 제외, "해결됨"·"닫힘"·"해소"·"확정" 등으로 시작하는 항목 제외).
- **재현 방법**: 다음 회차에 이 문서를 갱신하려면, `docs/ISSUES.md`에서 상태가 "열림"인 항목을
  다시 전수 추출하고(정규식: `### I-\d+` 헤더 뒤 `**상태**: 열림`으로 시작하는 블록), 그중
  **이번 문서에 이미 분류된 번호는 판정을 재사용**하고 신규 번호만 아래 §1 기준으로 새로 분류한다.

## 0. 이번 회차 분류에서 제외한 5건 — "닫힐 예정"이지 "닫혔다"고 가정하지 않는다

팀장 지시(27일차)에 따라 아래 5건은 **이번 회차 안에 다른 배정으로 종료될 예정**이라 분류
대상에서 뺀다. **닫혔다고 가정하지 않고**, 회차 말에 팀장이 확정 목록을 알려주면 그때 실제
결과(진짜 닫혔는지, 부분 해소인지)를 반영해 다음 회차 갱신에서 분류하거나 뺀다.

| 이슈 | 처리 예정 팀원 | 비고 |
| --- | --- | --- |
| I-075 | CORE + DESIGN | 관리자 지정/해제 UI 구현(D-076 정책은 26일차에 이미 확정) |
| I-130 | BOARD | 동시 일정 변경 제안 상호배제(트리거 차단) |
| I-133 | CREW(본인) | 배정 3에서 원시 JSON 보존으로 **실제로 해소 완료**(`permission-baseline.md` §10, `permission-baseline-raw-27.md` 신규) |
| I-134 | CREW(본인) | 배정 1에서 이미 해소함(D-077, `permission-baseline.md` §9) — **실제로 완료됨** |
| I-136 | DESIGN | **같은 회차 안에 실제로 해소됨(후속 갱신)** — 최초 §0 등재 시점엔 "조건부 (B), 실사용자 재현 확정되면 (A) 후보"였으나, DESIGN이 CDP 실좌표 클릭·터치 탭 양쪽으로 **실사용자 영향을 확정**했고(`elementFromPoint`가 제출 버튼 좌표에서 탭바 아이콘을 반환), 원인이 한 페이지 국소 결함이 아니라 **셸 전략 자체의 결함**(`min-h-0 flex-1` 박스의 `padding-bottom`을 넘친 콘텐츠가 밟고 지나감)임을 유사 폼 전수 조사로 밝혀 `/crews/[id]/settings`·`/notifications`·`/meetups/[id]` 등 4곳 모두 같은 회차에 수정했다. **분류 관점에서 중요한 사례** — §1에 "판정 전" 갈래를 추가한 계기(아래) |

**남은 41건**(46 − 5)을 아래 §1~§5 기준으로 분류한다.

## 1. 분류 기준(재사용 가능한 기준 — 이 절이 이 문서의 핵심 가치다)

| 분류 | 정의 | 판정 신호 |
| --- | --- | --- |
| **(A) 릴리스 차단** | 실사용자 영향이 있거나 v1.0 요구사항(FR/NFR/AC)을 미충족 | 실제 화면·데이터에 잘못된 결과가 나온다, 요구사항 원문의 AC가 실측과 어긋난다, 또는 **AC 충족 여부 자체가 검증되지 않았고 그 검증을 막는 것이 구조적 한계가 아니라 일시적 제약이다** |
| **(B) 알고 지낼 것** | 도구·환경 한계, 구조적으로 피할 수 없는 것, 또는 영향 낮음으로 이미 판정된 MINOR | 재현해도 실사용 경로가 없다/이미 팀이 "저위험·미수정"으로 판정했다/개발 도구(Turbopack·tsc·MCP)의 알려진 한계다 — **"토큰·계산은 맞다"가 "화면에 정확히 반영된다"를 대신하지 않는다: 값 검증과 렌더 검증은 다른 층이다** |
| **(판정 전)** | **재현·측정이 아직 미확정인 항목** — (B)가 아니다 | "실사용자 재현은 안 됐다"·"미검증"이라는 사실만으로는 (B)에 넣지 않는다. 확정 전까지 (A) 가능성을 닫지 않고, 확정되는 즉시(어느 쪽이든) 재분류한다 |
| **(C) 외부 입력 대기** | 사용자 결정·계정·요금제 등 **팀이 자체적으로 닫을 수 없는 것** | 코드를 아무리 고쳐도 "누가 무슨 값을 확정해 주는가"가 없으면 닫히지 않는다 |

**"판정 전"을 별도 갈래로 두는 이유(27일차 교훈, 두 건 독립 확인)**: 이번 회차 CREW가 (B)로
분류했던 I-032(원자 컴포넌트 합성 렌더 미검증)와 I-136(탭바 클릭 가로챔, 당시 "Playwright
한정일 수 있다"는 유보)가 각각 다른 경로로 재검증됐다 — I-032는 팀장의 반박으로, I-136은
DESIGN의 실측(CDP 실좌표 클릭·터치 탭 양쪽 재현 확정)으로. **두 건 다 실제로는 (A)였다** —
I-136은 한 페이지 국소 결함도 아니고 **셸 전략 자체의 결함**(`min-h-0 flex-1` 박스의
`padding-bottom`을 넘친 콘텐츠가 밟고 지나감)이라 `/crews/[id]/settings`·`/notifications`·
`/meetups/[id]` 등 4곳에 동시에 있었다. "미검증"·"재현 미확정"을 안전하게 (B)로 두는 습관이
두 번 연속 틀렸다는 것은 우연이 아니라 **패턴**이다 — 그래서 이 분류표 자체에 "판정 전"을
추가해, 다음부터는 미확정 항목이 기본값으로 (B)에 안착하지 않고 확정될 때까지 눈에 띄게
남도록 한다.

**적용 순서**: ① 이미 팀 판정(저위험·수용·의도된 범위 등)이 문서에 명시돼 있으면 그대로
재사용해 (B)로 분류하고 재논쟁하지 않는다 — 단, 그 판정의 **근거 자체가 이번 회차에도 여전히
성립하는지**는 확인한다(§3-A 참고, "도구 제약"을 근거로 들 때 특히 그렇다). ② 남은 것 중
"코드로는 못 닫고 사람의 결정/자산이 필요한가"를 먼저 걸러 (C)로 분류한다. ③ 나머지 중
실사용자에게 실제로 잘못된 동작·데이터가 보이는가, **또는 요구사항이 요구하는 검증(AC) 자체가
아직 수행되지 않았고 그 미수행이 구조적으로 불가피한 게 아니라면** (A)로 분류한다 —
"이론적으로 가능하다"·"드물게 발생할 수 있다"만으로는 (A)에 넣지 않지만, "검증을 안 했다"를
"문제 없다"로 치환하지도 않는다. **(A) 0건은 목표가 아니라 판정 결과일 뿐이다** — 실제로 나오면
정직하게 보고한다.

## 2. 분류 결과 요약

| 분류 | 건수 |
| --- | --- |
| (A) 릴리스 차단 | **1건** |
| (B) 알고 지낼 것 | **30건** |
| (C) 외부 입력 대기 | **10건** |
| 합계(제외 5건 뺀 나머지) | **41건** |

**(A)가 1건인 이유(27일차 팀장 검증으로 정정)**: 최초 분류는 CRITICAL·MAJOR급 결함
(I-089·I-101·I-102·I-103·I-107·I-111·I-114 등)이 발견 즉시 그날 안에 고쳐져 온 이력을 근거로
(A) 0건이라 판단했었다. 팀장이 세 항목(I-078·I-136·I-032)에 대해 (B) 판정의 근거를 재검증했고,
그중 **I-032가 (B) 기준(§1)을 충족하지 못한다**고 정정됐다 — 상세는 §3. I-078은 D-050의
확정 해석에 근거해 (B) 유지가 맞다고 확인됐다. **I-136은 §0에서 조건부(DESIGN 판정 대기)로
표시해 둔 대로 대기하는 사이 DESIGN이 실제로 실사용자 재현을 확정했고, 같은 회차 안에 셸
전략 수정으로 해소됐다**(§0 표 갱신 참고) — 결과적으로 두 건(I-032·I-136) 다 최초 (B) 판정이
틀렸다는 것이 같은 회차 안에서 드러난 셈이다.

## 3. (A) 릴리스 차단 — 1건

| 이슈 | 제목(요약) | 왜 (A)인가 | 어떻게 닫히는가 |
| --- | --- | --- | --- |
| I-032 | 원자 컴포넌트(폼 오류·Dialog/Drawer 오버레이·Toast)의 라이트·다크 **합성 상태** 실제 렌더가 NFR-018(대비)·Task 013 AC 기준으로 검증되지 않음 | CREW가 최초 (B)로 분류하며 든 근거("도구 제약(공유 Playwright 세션)으로 인한 미검증, 토큰은 기존 측정값 재사용이라 위험 낮음")를 팀장이 재검증한 결과 두 가지가 무너졌다: ① 브라우저 세션 점유는 **구조적으로 피할 수 없는 것이 아니라 일시적 제약**이다 — 25일차 운영 규칙 3("격리 Chromium 사용")이 이미 있고, **같은 27일차 회차 안에서 DESIGN이 그 방법으로 실측을 수행 중**이라 측정 자체는 가능함이 반증되고 있다. ② "토큰 재사용이라 위험 낮음"은 **토큰 값** 층위의 논증인데, I-032가 미확인으로 남긴 것은 **합성 렌더**(Toast 배경 위 `--destructive` 텍스트, Drawer 오버레이 위 포커스 링) 층위다 — 서로 다른 층을 섞어 "미검증"을 "무결함"으로 취급한 것이었다. 이 프로젝트가 25·26일차에 "고쳤다는 주장"·"검증했다는 주장"을 실측 없이 받아들인 것을 거듭 무너뜨려 온 것과 같은 실패 패턴이다. | **닫는 방법이 이미 정해져 있다** — DESIGN이 이번 회차에 쓰는 격리 Chromium 실측을 폼 오류 상태·Dialog/Drawer 오버레이·Toast 3종 조합에 대해 한 번 더 수행하면 된다. 대비 기준 미달이 실측되면 토큰 조정, 충족되면 "해결됨"으로 닫는다 — 이번 회차나 다음 회차 배정으로 즉시 소진 가능한 성격이라 게이트가 막연해지지 않는다 |

## 4. (C) 외부 입력 대기 — 10건, "누가 무엇을 결정/제공해야 닫히는가"

| 이슈 | 제목(요약) | 누가 | 무엇을 |
| --- | --- | --- | --- |
| I-033 | 핸들 형식 규칙(길이·허용 문자) 미정 | 사용자 | 최소·최대 길이, 허용 문자셋(대문자·한글·이모지 허용 여부)을 확정 — DB CHECK 제약으로 굳히기 전에 필요, 굳힌 뒤 바꾸면 기존 핸들 소급 문제 |
| I-034 | bio 상한 미정 | 사용자 | 한 줄 소개 글자 수 상한을 확정 — 마찬가지로 DB 제약 전 확정 필요 |
| I-038 | 크루명·소개 상한·금칙어 목록 미정 | 사용자(+법무) | 크루명·소개 글자 수 상한, 그리고 특히 **금칙어 목록과 우회 표기(초성·유니코드 치환) 대응 정책** — 신고 연동(FR-080/082, v0.2) 여부까지 포함해 확정 |
| I-039 | 권한 매트릭스에 "채팅방 열람" 행 없음 | 사용자(요구사항 소유자) | "읽기 전용 크루원"류 역할을 v0.2에 넣을지 결정 — 넣는다면 `chat:read`를 매트릭스에 별도 행으로 추가해야 함 |
| I-040 | 2.4절 다이어그램에 신청 철회 전이 없음(FR-022 E4와 불일치) | 사용자(요구사항 소유자) | 다이어그램에 철회 전이를 공식 반영하고, 철회 후 재신청 가능 여부(현재는 `rejected`로 근사)를 확정 |
| I-041 | 홈 대시보드 요약 표시 개수·크루 필터 범위 미정 | 사용자 | 다가오는 Meetup 요약이 몇 건을 보여줄지(현재 잠정값 5), `/calendar` 크루 필터를 이 요약에도 적용할지 확정 |
| I-043 | 모임 제안 날짜 검증 기준 타임존 미정 | 사용자 | "오늘"을 판정할 기준 타임존을 명시 확정(현재 `Asia/Seoul` 고정은 D-011에서 끌어온 추론일 뿐, 고객이 명시한 값이 아님) — 해외 체류 사용자 지원 여부와 결부 |
| I-055 | NFR-028(오류 추적) 도입이 Sentry 계정·DSN 부재로 미완결 | 사용자(계정 소유자) | Sentry 계정 생성 + 프로젝트 DSN 제공(또는 대체 도구로 방향 재결정) — 현재는 구조화 `console.error` 임시 구현으로 대체 중 |
| I-057 | 비밀번호 재설정 이메일 템플릿(PKCE `token_hash`) 대시보드 설정 미확인 | 사용자(Supabase 대시보드 접근 권한 보유자) | 대시보드에서 이메일 템플릿이 `token_hash`+`type=recovery` 형태로 설정돼 있는지 확인/구성 — MCP 도구로 조회 불가한 대시보드 전용 설정. **미확인 상태면 FR-003이 실사용자에게 조용히 깨져 있을 수 있어 10건 중 우선순위가 가장 높다.** **(C) 유지 근거(27일차 팀장 질의에 답함)**: 이 이슈는 두 층이 섞여 있다 — ① `/auth/confirm` 라우트가 `token_hash`+`type`을 올바르게 처리하는가는 **팀이 코드·타입 수준으로 이미 검증했다**(Task 039, `account-lifecycle-039.md`) — I-032와 달리 이 부분은 팀 안에서 닫을 수 있었고 실제로 닫혔다. ② 그런데도 (C)로 남는 이유는 **남은 유일한 gap이 "대시보드가 실제로 그 템플릿으로 설정돼 있는가"이고, 이건 MCP 도구가 조회할 수 없는 대시보드 전용 설정이라 팀이 스스로 확인할 방법이 없기 때문**이다 — 실제 이메일 왕복 클릭도 검증 수단이 될 수 있지만 레이트리밋 있는 실 SMTP 반복 발송이 필요해 이번 회차 범위로 자체 소진하지 않았다. 즉 I-032(팀이 안 한 것)와 다르게 I-057은 **팀이 할 수 있는 부분은 이미 다 했고, 남은 것만 외부 접근이 필요**하다. |
| I-094 | Realtime 월과금 추정치·실제 요금제(Free/Pro) 미확인 | 사용자(결제 정보 보유자) | Supabase 프로젝트의 실제 요금제를 대시보드에서 확인해 팀에 전달 — MCP 도구 세트(`get_advisors`·`list_tables`·`execute_sql` 등)에 결제 플랜 조회 수단이 없어 팀이 자체 확인 불가 |

## 5. (B) 알고 지낼 것 — 30건, 분류 근거(한 줄씩)

**이미 팀 판정을 재사용(재논쟁하지 않음)**:

- **I-071** — TS/SQL 이중화. "해소 불가로 수용"이 이미 확정 판정(D-032 근거).
- **I-108** — `chat_room_reads.last_read_at` 자기 위조. 이미 "저위험·자기 한정 영향, 미수정" 판정.
- **I-125** — `create_report` 자기신고(post/comment/chat) 미차단. 이미 "실증됨·실해악 낮음, 미수정" 판정.
- **I-126** — 신고 레이트리밋 없음. 이미 "관측만·우선순위 낮음" 판정.
- **I-131** — 게시글 필드 raw REST 사후 변경. 이미 "이론적 표면, 실사용 경로 없음"으로 판정.
- **I-132** — `generate_typescript_types` nullable 미표현. 이미 "우회 적용됨, 타입 표현 문제일 뿐"으로 판정.

**도구·개발 환경의 구조적 한계(팀이 고칠 수 있는 대상이 아님 — I-032와 달리, 이 5건은 대안이
없거나 대안 실행 비용이 문제 자체보다 크다는 점을 27일차에 재확인했다)**:

- **I-048** — Turbopack 영속 캐시가 `/mnt/e`(WSL2 drvfs)에서 불안정. 저장소 위치 자체의 물리적 제약.
- **I-051** — `apply_migration`이 로컬 파일을 안 만듦. Supabase MCP 도구 자체의 동작(팀이 바꿀 수 없음), 절차로 인지하며 대응 중.
- **I-129** — 병행 `npm run dev` 중 `tsc --noEmit`이 의미 분석을 건너뜀. Next.js `next-env.d.ts` import 구조 + 공유 개발 환경의 조합, 우회법(임시 tsconfig)이 이미 확립됨.
- **I-135** — Turbopack이 WSL2 drvfs에서 HMR을 놓침. 저장소 위치의 물리적 제약, 팀 운영 관례(dev 서버 재시작)로 대응.

**실사용 경로가 없거나 영향이 낮음(재발 방지 확인은 되어 있음)**:

- **I-019** — 팀 규모 미정으로 일정 산출 불가. 팀 편성이 이미 실행됐다(`SCHEDULE.md`가 단일 소스) — 문서 잔재, 릴리스와 무관.
- **I-020** — ThemeProvider 명시 토글 없음(Task 011 이월). NFR-022(라이트·다크 둘 다 동작)는 OS 폴백으로 이미 충족, 명시 토글 자체는 기능 확장이지 결함이 아님.
- **I-021** — Base UI 프리미티브 재검토 가능성. 실제 문제 사례 0건, 관찰뿐.
- **I-022** — shadcn 컴포넌트 추가 시 수동 메모 확인 필요. 반복 확인 절차이지 결함 아님, 위반 사례 없음.
- **I-023** — PRD §8 shadcn 표기가 "도입 예정"에 머묾. 문서 동기화 문제, 기능 영향 없음.
- **I-024** — `Notification.payload` 타입 미분리. 알림 소비 컴포넌트 완성 이후에도 실사용 결함 보고 없음.
- **I-026** — 인증 세션 조회 계층 위치 미확정. 컨벤션상 애매함일 뿐 동작 정상(I-047과 같은 결).
- **I-028** — 다크 토큰 두 블록 자동검증 안 됨. R-002(CI 부재)의 연장, 실제 불일치 발견 사례 없음.
- **I-029** — `--destructive`와 `--crew-12` 색상 유사. 저영향 디자인 미세조정, 기능 결함 아님.
- **I-031** — Mock 시드 "투표 40" 문서 3곳 오기. 실제 구현은 이미 올바른 값(72)을 쓴다 — 순수 문서 정정 잔여.
- **I-047** — Supabase 클라이언트 팩터리 위치가 컨벤션과 어긋남. ESLint zone 예외로 이미 우회, 동작 정상 — 구조 정리는 내부 리팩터링 과제.
- **I-050** — CLAUDE.md D-030③ 문구 오독 소지. 문서 표현 문제, 동작 영향 없음.
- **I-052** — 인증 보호 라우트 `notFound()`가 HTTP 200 응답. 화면은 정상(소프트 404), noindex로 색인 문제도 완화됨 — I-044(D-040으로 이미 같은 결로 수용)와 동급.
- **I-054** — `createJoinRequest`·`createPoll`이 다중 PostgREST 호출로 원자성 없음. 발견(18일차) 이후 **모든 신규 쓰기 경로에 "I-054 회피 원칙"을 적용**(042A·042B·040 등 전부 단일 RPC로 설계) — 원 2개 함수만 미수정으로 남았고 9일간 실제 발생 사례 0건.
- **I-056** — 탈퇴 파기가 Admin API 아닌 직접 SQL. 실질 결과(로그인 차단·이메일 익명화)는 의도한 효과와 동일, 재구성 비용 대비 이득 낮음.
- **I-059** — 게스트 접근 시 오진단 예외 로그. 화면·리다이렉트 정상, 로그 잡음 문제일 뿐.
- **I-063** — `postLinkCard` 미충전. 실사용 경로(post_link 쓰기) 자체가 앱에 없어 현재 영향 0.
- **I-078** — 신고 복합 처리(삭제+제재) 불가. **27일차 팀장이 D-050 원문("채택하지 않은 대안" 절)을 직접 대조해 재확인**: FR-082 AC1을 상호배타 3갈래로 읽은 것은 요구사항 미충족이 아니라 **이미 확정된 해석 뒤에 남은 "알려진 한계"**다 — 확정된 결정(D-\*)은 재논쟁 대상이 아니므로 (B) 유지.
- **I-082** — 알림 realtime 최초 구독 1회 실패, 자연 회복. 이미 "낮음"으로 판정, 콘솔 오류만·화면 정상.
- **I-091** — "self-service 컬럼값 미제한" 패턴 정식화. 결함 항목이 아니라 방법론 기록(다음 회차 조사 지침) — 분류 대상 자체가 아니지만 열림 상태라 포함.

**참고(제외 목록, §0)**: I-075는 D-076(정책 확정)만 남은 상태라 이 회차 분류 시점엔 (B) 성격이
유력해 보였고, 이번 회차 안에 CORE+DESIGN이 처리하므로 §0에 따라 분류 표에는 넣지 않았다.
**I-136은 처리 결과가 이미 나왔다** — §0 표 갱신 참고: (B) 성격이 아니라 실제로는 (A)(실사용자
영향 확정, 셸 전략 결함)였고 같은 회차 안에 DESIGN이 해소했다.

## 6. v1.0 릴리스 종료 게이트

**종료 조건**: **(A) 릴리스 차단 0건 + (C) 외부 입력 대기 전건 결정됨.**

- **지금 이 시점엔 (A)가 1건(I-032)이라 종료 조건이 아직 충족되지 않는다.** 게이트의 가치는
  "지금 0건"이 아니라 "무엇이 차단인지 다투지 않고 합의됐다"는 데 있다 — I-032는 §3에 적힌
  방법(격리 Chromium 실측 1회)으로 이번 회차나 다음 회차 안에 소진 가능하다.
- **(A)는 매 회차 이 문서를 갱신할 때 재확인한다** — 새 이슈가 (A)로 분류되면 그 즉시(이
  프로젝트의 기존 관행대로) 고치고 재분류하지, "릴리스 후 처리"로 미루지 않는다.
- **(C)는 10건 전부 사용자(또는 계정 보유자)의 결정·제공이 있어야 닫힌다** — 위 §4 표가 팀장이
  사용자에게 요청할 목록이다. 이 10건이 0이 되면(결정되어 코드/설정에 반영되고 각 이슈가
  "해결됨"으로 갱신되면) 게이트의 두 번째 조건이 채워진다.
- **(B)는 종료를 막지 않는다.** 30건은 v1.0 출시 이후에도 **문서(`docs/ISSUES.md`)로 승계**되며,
  "닫힌 것"이 아니라 "알고 지내기로 한 것"이다 — 릴리스 판정과 무관하게 상태는 계속 "열림"으로
  남아도 된다. 다음 회차 이후 새로 발견되는 이슈가 이 표의 어느 판정과 같은 모양이면(예: 또 다른
  트리거 함수가 다중 PostgREST 원자성 문제를 반복하면 I-054와 같은 결) 재논쟁 없이 같은 분류를
  적용한다.
- **재분류가 필요한 신호**: (B)로 분류된 항목이라도 **"도구 제약"을 근거로 들 때는 그 제약이
  이번 회차에도 실제로 피할 수 없는지 다시 확인한다**(I-032가 이 확인을 건너뛰어 재분류된
  사례 — §3). (B)로 분류된 항목이라도 실사용자 재현이 확정되면 즉시 (A)로 재분류하고 이 문서와
  `docs/ISSUES.md` 양쪽을 갱신한다 — **§0의 I-136이 정확히 이 경로를 이번 회차 안에 실제로
  밟았다**: "조건부 (B)"로 대기하던 중 DESIGN이 CDP 실좌표 클릭·터치 탭으로 실사용자 재현을
  확정했고, 원인이 셸 전략 자체의 결함(`min-h-0 flex-1` 박스의 `padding-bottom`을 넘친 콘텐츠가
  밟고 지나감)임이 밝혀져 4개 페이지 동시 수정으로 같은 회차에 해소됐다 — 미확정 항목을 (B)로
  안착시키는 습관의 위험을 I-032와 독립적으로 재확인한 사례라 §1에 "판정 전" 갈래를 추가했다.

### 종료 조건의 D-\* 승격 여부

이 종료 조건("(A) 0건 + (C) 전건 결정")은 앞으로도 반복 적용될 **확정된 팀 규칙**이라 D-\*로
등재하는 것이 맞다고 판단했다. 번호는 **D-080**이다.

27일차 최종 배치 — **D-077** CREW(I-134 EXECUTE 관례) · **D-078** CORE(관리자 회수 최소 1명
보장) · **D-079** BOARD(I-130 상호 배제) · **D-080** 이 종료 게이트 · **D-081** DESIGN(앱 셸
하단 여백 예약 규약). 이 문서 초판의 귀속 표기(D-078=BOARD, D-079=CORE)는 최종본이 아니다 —
회차 중 배정이 다섯 번 교차했고, 최종 기준은 **"실제 DB와 코드에 이미 박힌 값"**이었다. 경위와
재발 방지안은 `docs/ISSUES.md`의 결정 번호 충돌 이슈에 있다.

본문은 회차 중 초안으로 두었다가 **팀장이 회차 마감에 `docs/prioritization-and-risks.md` 6.3절로
병합했다**(D-080). 이 문서(`issue-triage-release-readiness.md`)는
그 규칙을 "지금 이 시점에 적용한 결과"이고, D-080은 "규칙 자체"라 두 문서의 역할이 겹치지 않는다.
