# 03. CREW 업무 분담

담당 **15건** · 총 **85.9인일** · **37주차 이후(Task 042B, 팀장 재배정)**까지이며, 크리티컬 패스 위의 Task는 **005** 1건뿐이다. 대신 **L 이상 Task를 9건**으로 4명 중 가장 많이 지고, `🚧` Task는 **030** 1건이다. 크리티컬 패스 밖에 있는 만큼 **다른 사람이 막혔을 때 예비 작업을 흡수할 여지**가 가장 큰 자리다.

## 이 문서의 위치와 규칙

- **스코프의 단일 소스는 [`ROADMAP.md`](../ROADMAP.md)다.** Task 제목·구현 사항·수락 기준이 어긋나면 ROADMAP이 이긴다
- **사람(역할·강점·가용 인일)의 단일 소스는 [`docs/team/03.CREW.md`](../../team/03.CREW.md)다**
- **전체 일정(팀 편성·크리티컬 패스·버퍼·균등 검증)의 단일 소스는 [`docs/SCHEDULE/SCHEDULE.md`](../../SCHEDULE/SCHEDULE.md)다.** 여기에 복제하지 않는다
- 이 문서는 **이 사람의 담당 Task와 그 공수·주차·선행 대기만** 적는다
- **이 디렉터리는 `docs/ROADMAP/` 아래에 있지만 `ROADMAP.md`를 고치지 않는다.** 디렉터리 위치가 소유권을 뜻하지 않는다
- **`A`/`B` 접미가 붙은 Task는 XL 분할 단위이며 ROADMAP의 Task 정의가 아니다.** 원 번호를 함께 적는다

---

## 담당 Task

- **Task 004: 컴포넌트 기반 도입(shadcn/ui) 결정과 초기 설치**
  - 팀·리뷰: A팀 / 리뷰 BOARD(B팀)
  - 공수: 1.8인일 (S) — **`PRD-validation.md` §6.2 미계상 항목이라 불릿 기반으로 보완**: 불릿 3개 × 0.5 = 1.5 × 착수 준비 1.2 = 1.8. §6.3은 shadcn을 "단축 지렛대 ①"로만 언급하고 도입 비용을 계상하지 않았다
  - 기간: 1주차 (2026-07-27 ~ 2026-08-02)
  - 선행 대기: 없음
  - 참조: **D-036**, R-020, NFR-017·020
  - 의존: 없음
  - 상태: 완료 (1일차, 2026-07-24)
- **Task 005: 라우트 구조와 페이지 골격 생성**
  - 팀·리뷰: A팀 / 리뷰 DESIGN(B팀)
  - 공수: 2.8인일 (M) — **§6.2 미계상 항목이라 불릿 기반으로 보완**: 불릿 5개 × 0.5 = 2.5 × 공통 기반 1.1 ≈ 2.8. §6.2는 이 비용을 「레이아웃 4.0」에 내재시켰으나 19개 페이지 골격 생성은 별도 작업이다
  - 기간: 2주차 (2026-08-03 ~ 2026-08-09)
  - 선행 대기: Task 001(CORE) — 2026-08-02 종료 예정
  - 참조: PRD §5·§6·§10, **D-011**, **CON-02**, R-001, SC-01~20
  - 의존: Task 001 (암묵 — 디렉터리 규약이 라우트 배치를 결정한다)
  - 상태: 완료 (2일차, 2026-07-24)
- **Task 009B: 비즈니스 규칙 순수 함수 모듈 — 권한 매트릭스·멤버십 전이·색 배정** (원 Task 009)
  - 팀·리뷰: A팀 / 리뷰 BOARD(B팀)
  - 공수: 4.5인일 (M) — §6.2가 기능 영역에 계상한 것을 ROADMAP 기준으로 되돌린 값이다: 크루·멤버십 「멤버십 상태 전이 + 권한 매트릭스(3.3절 34행) 순수 함수 구현·대조」 3.0 + 캘린더 「색 배정 해시 + 같은 날짜 충돌 회피」 1.5 = 4.5
  - 기간: 3~4주차 (2026-08-10 ~ 2026-08-23)
  - 선행 대기: Task 002(DESIGN) — 2026-08-02 / Task 006(CORE) — 2026-08-09
  - 참조: **NFR-036**, **D-002**·**D-006**·**D-026**, NFR-025, D-029, R-015
  - 의존: Task 002, Task 006
  - 상태: 완료 (3일차, 2026-07-24)
- **Task 008: 실시간 구독 인터페이스 추상화와 Mock 이벤트 소스**
  - 팀·리뷰: A팀 / 리뷰 DESIGN(B팀)
  - 공수: 2.5인일 (M) — §6.2 공통 「**구독 인터페이스 추상화 + Mock 이벤트 소스** (⑨ M-8)」 2.5인일 직접 매핑. §6.2가 "문서에 없으나 필수"로 적어 둔 항목이며 ROADMAP은 이를 Task로 만들었다
  - 기간: 4주차 (2026-08-17 ~ 2026-08-23)
  - 선행 대기: 없음
  - 참조: **D-030 ②**, **D-023**, NFR-007·008·009, R-011, R-019, CON-12
  - 의존: Task 001, Task 006
  - 상태: 완료 (3일차, 2026-07-24)
- **Task 010: Mock 시드 데이터 생성**
  - 팀·리뷰: A팀 / 리뷰 BOARD(B팀)
  - 공수: 3.0인일 (M) — §6.2 공통 「Mock 시드 데이터 (크루 15·멤버 300·게시글 200·투표 40·메시지 2,000·Meetup 60)」 3.0인일 직접 매핑. **수치 정정(4일차)**: "투표 40"은 FR-060의 가결 Poll:Meetup 1:1 제약과 모순되는 오기다(40 < Meetup 60이라 동시 성립 불가) — 실제로는 투표 72(가결 60 포함) 생성. 근거는 **I-031**
  - 기간: 5주차 (2026-08-24 ~ 2026-08-30)
  - 선행 대기: Task 009A(BOARD) — 2026-08-23 종료 예정
  - 참조: **R-017**, D-031, D-014, NFR-035, R-003
  - 의존: Task 006, Task 009A·009B (원 Task 009)
  - 상태: 완료 (4일차, 2026-07-24)
- **Task 015B: 인증·계정 화면 (F001~F004) — 계정 설정·핸들 검색** (원 Task 015)
  - 팀·리뷰: A팀 / 리뷰 DESIGN(B팀)
  - 공수: 7.5인일 (L) — §6.2 인증·계정 「계정 설정 3.0 + 핸들 검색 필드·결과 2.5 + 4상태 + `/sample` 등록 2.0」 = 7.5
  - 기간: 7~8주차 (2026-09-07 ~ 2026-09-20)
  - 선행 대기: Task 013(DESIGN) — 2026-09-06 종료 예정
  - 참조: FR-004·006, **D-005**, NFR-013·014, **R-012**, SC-19
  - 의존: Task 013
  - 상태: 완료 (6일차, 2026-07-24)
- **Task 016B: 크루 탐색·개설·크루 홈 (F005~F008, F011) — 크루 개설·크루 홈** (원 Task 016)
  - 팀·리뷰: A팀 / 리뷰 BOARD(B팀)
  - 공수: 7.5인일 (L) — §6.2 크루·멤버십 「크루 개설 3.0 + 크루 홈 4분기 + 가입 신청 버튼 상태 기계 4.5」 = 7.5
  - 기간: 8~10주차 (2026-09-14 ~ 2026-10-04)
  - 선행 대기: 없음
  - 참조: FR-010·011·022, **D-008**·**D-014**·**D-016**, D-007, SC-07·09
  - 의존: Task 013
  - 상태: 완료 (7일차, 2026-07-24)
- **Task 016A: 크루 탐색·개설·크루 홈 (F005~F008, F011) — 크루 탐색** (원 Task 016)
  - 팀·리뷰: A팀 / 리뷰 DESIGN(B팀)
  - 공수: 6.0인일 (L) — §6.2 크루·멤버십 「크루 탐색 — 검색바·카테고리 필터·CrewCard/Grid·무한 스크롤·"가입됨" 배지·비로그인 상태(D-007)」 6.0인일 직접 매핑
  - 기간: 10~12주차 (2026-09-28 ~ 2026-10-18)
  - 선행 대기: 없음
  - 참조: FR-012·014, **D-007**·**D-017**, SC-08
  - 의존: Task 013
  - 상태: 완료 (8일차, 2026-07-24)
- **Task 017A: 멤버십 운영 화면 (F009·F010·F012~F015, F032) — 멤버 관리** (원 Task 017)
  - 팀·리뷰: A팀 / 리뷰 BOARD(B팀)
  - 공수: 7.0인일 (L) — §6.2 크루·멤버십 「멤버 관리 — 역할 정렬 목록, 초대 다이얼로그, 신청 승인/반려 탭, 임원 임명」 7.0인일 직접 매핑
  - 기간: 12~13주차 (2026-10-12 ~ 2026-10-25)
  - 선행 대기: 없음
  - 참조: FR-020·023·024·026, **D-002**·**D-005**, SC-14
  - 의존: Task 013, Task 016B(원 Task 016)
  - 상태: 완료 (9일차, 2026-07-24)
- **Task 017B: 멤버십 운영 화면 (F009·F010·F012~F015, F032) — 크루 설정·받은 초대함** (원 Task 017)
  - 팀·리뷰: A팀 / 리뷰 DESIGN(B팀)
  - 공수: 6.0인일 (L) — §6.2 크루·멤버십 「크루 설정 4.0 + 받은 초대함 2.0」 = 6.0
  - 기간: 14~15주차 (2026-10-26 ~ 2026-11-08)
  - 선행 대기: 없음
  - 참조: FR-021·028·012·062, **D-002**·**D-016**, SC-15·20
  - 의존: Task 013, Task 016B(원 Task 016)
  - 상태: 완료 (10일차, 2026-07-24) — 오너 이양·해산은 스코프대로 Task 040(v0.2)으로 이월, leave-crew 하드코딩 유지
- **Task 030: 인증 연결과 계정 잠금 구현**
  - 팀·리뷰: A팀 / 리뷰 BOARD(B팀)
  - 공수: 7.5인일 (L) — §6.4 「Auth 통합 + **계정 잠금 자체 구현**(C-3) + 커스텀 SMTP(C-4) 6~9」 **중앙값 7.5**
  - 기간: 29~30주차 (2027-02-08 ~ 2027-02-21)
  - 선행 대기: Task 029A·029B(CORE) — 2027-02-07 종료 예정. **추가로 I-016이 2027-01-25까지 해소돼야 착수 가능하다**
  - 참조: FR-001·002, **D-020**·**D-021**, NFR-010·016, **I-016**(차단), CON-06·CON-11
  - 의존: Task 029A, Task 029B (원 Task 029)
  - 상태: 완료 (17일차, 2026-07-25) — I-016은 팀장이 Resend로 확정(D-042)해 해소. `get-auth-session.ts`·
    로그인/가입/온보딩/로그아웃 Server Action을 실 Supabase Auth로 교체. `auth_attempts`(Task 028이
    이미 만들고 029A가 완전 거부 RLS까지 끝내 둔 상태)를 `evaluateLoginLockout`(기존 순수 함수)과
    연결해 D-020 계정 잠금을 실측 검증(5회 연속 실패 → 6회째 자격 증명이 맞아도 거부, Playwright).
    NFR-010 실측 중 `@supabase/ssr` 기본 쿠키가 `httpOnly:false`임을 발견해 `cookieOptions`로 수정·
    재검증(수정 전 `document.cookie`에 세션 토큰 노출 → 수정 후 미노출). 실 테스트 계정 2개 생성.
    1차 시도(`src/lib/data/supabase/auth.ts`)는 팀장이 CON-05·CON-06 위반으로 반려해
    `src/lib/auth/`(신설, `src/lib/realtime/**`와 대칭, `eslint.config.mjs` zone 7)로 재구성했다 —
    `server.ts` httpOnly 수정은 유지. **FR-002(로그인·로그아웃·세션 유지)는 끝까지 구현
    완료다. FR-001(회원가입)은 구현했으나 프로필 생성에서 끊긴다** — `createProfile`(Task 032
    소관 쓰기 경로)이 `id`를 받지 않아 신규 가입자의 `public.profiles` 행이 실 DB에 생기지
    않고, 그 결과 `getAuthSession()`이 `forbidden`을 반환해 **신규 가입자는 로그인해도 앱을
    쓸 수 없다**(게스트와 동일 취급). 이번 회차의 실 테스트 계정 2개는 회원가입 폼이 아니라
    Admin REST + 프로필 SQL 직접 삽입으로 만들었다. Task 032가 `createProfile`에 `id` 매개변수를
    추가해야 FR-001이 end-to-end로 완결된다 — 다음 회차 착수 전 반드시 확인할 것. 남은 한계
    (온보딩 완료 컬럼 부재)와 `npm run build` 잔여 이슈(경로 B, CORE 소관, 이미 해소됨)는
    `docs/decisions/auth-integration-030.md` 참고.
- **Task 039: 계정 생애주기 (FR-003, FR-005)**
  - 팀·리뷰: A팀 / 리뷰 DESIGN(B팀)
  - 공수: 8.0인일 (L) — **`PRD-validation.md`에 산정이 없어 FR 단가로 보완**: 3.1인일/FR × FR-003·005 2건 = 6.2 × 익명화 데이터 처리 1.3 ≈ 8.0. 탈퇴 익명화가 20 엔티티 전반의 작성자 표기에 걸린다(**D-010**)
  - 기간: 31~32주차 (2027-02-22 ~ 2027-03-07)
  - 선행 대기: 없음
  - 참조: FR-003·005, **D-010**, NFR-031·032, D-021
  - 의존: Task 030 (암묵 — 인증 연결 이후)
  - 상태: 완료 (18일차, 2026-07-25) — 비밀번호 재설정(FR-003)과 회원 탈퇴(FR-005)를 구현했다. 마이그레이션 4건(`profiles_add_deactivation_grace_period`·`account_deactivation_restore_functions`·`anonymize_expired_deactivated_profiles_job`·`fix_anonymize_job_issue_reference`), Server Action 4개(`request-password-reset`·`confirm-password-reset`·`deactivate-account`·`restore-account`), 라우트 핸들러 `src/app/auth/confirm/route.ts`(Supabase PKCE `token_hash`+`type`) + `/auth/confirm-error`, 페이지 3종(`/reset-password`·`/reset-password/confirm`·`/account/restore`), 컴포넌트 7개. 신규 결정 **D-044**(탈퇴를 `deactivated` 30일 유예 → `withdrawn` 파기 완료 2단계로 모델링). **익명화 실측**(`begin`…`rollback`, 시드 계정): `handle='withdrawn-<8자>'`·`display_name='탈퇴한 사용자'`·`avatar_url`/`bio`=null·`status='withdrawn'`·`anonymized_at` 기록, `auth.users`는 이메일 익명화 + `banned_until='infinity'`. **D-010 익명화 범위가 `profiles` 한 테이블로 충분함을 DESIGN이 독립 검증** — `profiles`를 FK 참조하는 **19개 테이블 전체**와 `mappers.ts` 전량 대조 결과 작성자 표기를 복제한 테이블 **0건**, `notifications.payload`는 ID·열거값뿐이고 `audit_logs`엔 JSON 페이로드 컬럼 자체가 없다. NFR-031 30일 경계 실측: 29일 → `purged_count=0` / 31일 → `1`. FR-005 AC1 실측: 활성 오너 크루 보유 시 `{ok:false, reason:'owns_active_crew'}`. D-003 정합성은 `poll_votes` 미변경으로 구조적 보장. cron 3잡 시간 미겹침(18:00·18:30·19:00 UTC). **DESIGN 교차검증 minor 5건 전건 해소**: ① 핸들 검색이 유예 중 사용자를 실명·실아바타로 노출하던 것 → `projectHandleSearchResult`에 `status !== "active"`를 `searchOptOut`과 **한 조건으로 합류**(R-012 불변식 유지: 미존재·옵트아웃·비활성이 동일한 `{found:false}`) ② Mock `deactivated` 픽스처 추가(NFR-035) ③ 30일 경계 조건 미세 중첩은 **고치지 않고 문서화**(하루 1회 cron + 조건부 UPDATE라 데이터 손상 경로 없음) ④ `/auth/confirm` 신설이 FR-001 결손을 해소했다는 **근거 없는 인과 주장 철회**(`auth-integration-030.md` 27~30행 원문은 "표준 동작, 별도 구현 불필요"이고 실제 한계는 `createProfile` id 미전달이었다) ⑤ "파기" 문언 정확화(`auth.identities`는 파기가 아니라 `banned_until` 무효화). 신규 미결 **I-056**(`auth.users`를 Admin API 아닌 직접 SQL로 수정)·**I-057**(재설정 메일 템플릿 대시보드 설정 미확인). **미완**: 로그인이 필요한 인증 왕복(탈퇴·복구 실클릭, 레이트 리밋 429 UI 발동)은 공유 Playwright 브라우저 프로필이 다른 세션에 잠겨 **팀원도 팀장도 검증하지 못했다** — 19일차 이월. 근거: `docs/decisions/account-lifecycle-039.md`.
- **Task 040: 크루 생애주기 (FR-013, FR-025, FR-027)**
  - 팀·리뷰: A팀 / 리뷰 BOARD(B팀)
  - 공수: 9.5인일 (L) — FR 단가 보완: 3.1인일/FR × FR-013·025·027 3건 = 9.3 ≈ 9.5
  - 기간: 34~36주차 (2027-03-15 ~ 2027-04-04)
  - 선행 대기: Task 032(DESIGN) — 2027-03-14 / Task 035(DESIGN) — 2027-01-17
  - 참조: FR-013·025·027, **D-002**·**D-003**·**D-009**·**D-015**, NFR-015·032
  - 의존: Task 032, Task 035 (암묵 — 해산 시 채팅 로그 파기가 D-009로 묶여 있다)
  - 상태: 완료 (19일차, 2026-07-25) — FR-024·FR-027 DB 기반(RLS·트리거)이 029B에서 이미
    구현돼 있었다는 것을 확인하고, 남은 gap 셋을 마이그레이션 3건으로 채웠다: ①
    `crews_guard_owner_only_fields` 확장(FR-025 E1, 오너 이양 대상이 active 멤버가 아니면
    SQL이 거부 — 원래 앱 레이어만 있던 검증을 SQL 강제 경계로 승격) ② 강퇴 시 진행 중 투표
    무효화 트리거 신설(FR-027 AC3, 029B가 이월한 항목) ③ `public.disband_crew` SECURITY
    DEFINER RPC 신설(FR-013, 크루 archived 전이 + 투표·Meetup 취소 + 채팅 즉시 파기를
    단일 트랜잭션으로 원자화, 운영 규칙 2). Server Action 3개(`transfer-crew-ownership`·
    `remove-crew-member`·`disband-crew`), 컴포넌트(`DisbandCrewForm` 신규, `MemberList`에
    이양·강퇴 다이얼로그 추가), 감사 로그 3종 추가(`audit-log.ts`, 팀장 승인 대기 중 최소
    diff로 선적용). **실측**(합성 데이터, 커밋 후 명시적 DELETE 정리): 10개 시나리오 전부
    PASS — 특히 앱 우회(비멤버 대상 직접 PATCH, `anon`의 RPC 직접 호출) 2건이 정확히
    거부됨을 확인했다. `npx tsc --noEmit`·`npm run lint` 통과. 새 결정 **D-046**(오너 이양
    화면은 크루 설정이 아니라 멤버 관리에 둔다). 미검증·이월: **I-066**(해산된 크루의
    게시판·채팅·설정 쓰기가 막히지 않음)·**I-067**(`CrewHomeContainer`의 archived 렌더링
    미검증)·실 브라우저 클릭 검증(Playwright 세션 공유 제약으로 이번에도 미완, Task 039와
    같은 한계). **후속 수정(같은 19일차)**: `disband_crew`가 `public.*`에 SECURITY DEFINER로
    직접 노출돼 `authenticated_security_definer_function_executable` WARN을 새로 만든 것을
    팀장이 실측 발견 — 029B의 `private.*` DEFINER + `public.*` INVOKER 얇은 래퍼 2단 구조로
    재구성(마이그레이션 `20260725093855`, 원본은 무변경)해 WARN을 없앴다. 재검증으로 동작
    불변 확인(`ok`/`reason` 3가지 케이스 동일). 함께 배정된 절차 정리 3건도 완료: 마이그레이션
    파일명 3건을 원격 version(`085454`·`085508`·`085528`)에 맞춰 rename, `crew.disbanded`의
    `targetId`를 `actorId`→`crewId`로 정정, **I-061**(`restore-account.ts`·
    `deactivate-account.ts`·이번에 만든 `disband-crew.ts` 3곳 모두 `redirect()` 앞에
    `refresh()` 누락 — Next 16 문서 확인 후 순서 수정) · **I-068**(탈퇴 확인 카피가 익명화
    시점을 명시하지 않던 것, `ko.ts` 문구만 수정) 해결. `get_advisors(security)` 최종
    WARN 1건(`auth_leaked_password_protection`, 무관한 기존 항목)만 남음.
    **I-067(FR-013 AC2) 브라우저 검증 FAIL 후속**: DESIGN이 해산된 크루의 캘린더에서
    과거 Meetup 바가 미래 바와 함께 완전히 사라짐을 실측(확정 결함으로 격상) — 근본 원인은
    `listCrewsByProfile`이 `crews.status='active'`로 크루 자체를 걸러 archived 크루가
    "내 크루" 목록에서 통째로 빠진 것. `includeArchived?: boolean` 옵션(기본 `false`,
    기존 호출자 4곳 전수 조사로 동작 불변 확인)을 추가해 데이터 레이어 절반을 마감,
    캘린더 렌더링(격자 미구분·상세 패널 배지·필터 목록 포함) 절반은 DESIGN에 시그니처를
    확정 통보. RLS(`crews`·`meetups` SELECT)는 실측 결과 이미 정상이라 변경하지 않았다.
    상세: `docs/decisions/crew-lifecycle-040.md`.
- **Task 042A: 신고·차단·관리자 콘솔 (FR-080~FR-082) — 신고·차단** (원 Task 042)
  - 팀·리뷰: A팀 / 리뷰 DESIGN(B팀)
  - 공수: 6.5인일 (L) — FR 단가 보완: 3.1인일/FR × FR-080·081 2건 = 6.2 ≈ 6.5
  - 기간: 36~37주차 (2027-03-29 ~ 2027-04-11)
  - 선행 대기: 없음
  - 참조: FR-080·081, **D-008**·**D-014**, NFR-015
  - 의존: Task 032 (암묵 — 쓰기 경로 이후)
  - 상태: 완료 (20일차, 2026-07-25) — `reports`·`blocks` 테이블·기본 RLS(본인 스코프)는
    Task 028·029A가 이미 만들어 둔 상태였다(`list_tables` 확인 후 새 테이블 생성 없이 정책·
    RPC만 추가). I-054 회피를 처음부터 지켜 신고·차단 쓰기를 각각 단일 RPC(`create_report`·
    `create_block`, security invoker)로 구현했다 — `create_report`는 `ON CONFLICT ... DO
    UPDATE`로 FR-080 AC1 중복 신고 병합을 원자적으로 처리하고(자기 신고·빈 사유는 테이블
    CHECK로 방어), `create_block`은 멱등(`already_blocked` 플래그)이며 자기 차단 금지는
    028부터 있던 CHECK가 강제한다. FR-081 AC2(차단자의 초대 차단, "사유는 노출하지 않음")는
    앱이 아니라 `private.is_blocked` SECURITY DEFINER 헬퍼 + `invitations_insert_staff_or_owner`
    RLS 확장으로 구현했다 — 클라이언트가 "상대가 나를 차단했는가"를 안전하게 조회할 방법이
    없기 때문이다. 부수적으로 `createInvitation`(`invitation.ts`)을 `DataResult` 반환으로
    바꾸고 `invite-crew-member.ts` 한 줄을 고쳤다(D-030 ③). 마이그레이션 4건 적용
    (`report_block_rpcs_042a` 외 3건 — 실측 중 발견한 UPDATE 정책 공백·15일차 anon EXECUTE
    교훈 재발·트리거 EXECUTE 노출을 그 자리에서 수정). 실측(전부 `begin`…`rollback`) 14개
    시나리오 전부 PASS — 신고 병합·자기 신고 거부·빈 사유 거부·차단 멱등·자기 차단 거부·
    차단자 초대 거부(+대조군 정상 초대)·anon 거부·self-service status 변경 거부/reason
    변경 허용. `get_advisors(security)` 최종 신규 WARN 0건. `npx tsc --noEmit`·`npm run lint`
    clean. 앱 레이어: `lib/rules`(`report-eligibility.ts`·`block-content-visibility.ts`)·
    `lib/data/supabase`(`report.ts`·`block.ts`, mock 없음 — Task 032 이후 전례)·`lib/actions`
    (`create-report`·`create-block`·`remove-block`)·`components/moderation`(`ReportDialog`·
    `BlockButton`·`BlockedContentNotice`·`BlockedUsersList`+`Skeleton`+`Container`). 크루원
    목록(`MemberList`)에 신고·차단 버튼을 배선했고(CREW 소유 도메인), `/settings`에 차단
    관리 섹션을 추가했다. 게시판·채팅의 콘텐츠 접힘(AC1)은 처음엔 다른 팀원 소유 도메인과의
    동시 작업 충돌을 피해 범위 밖으로 두고 **I-072**로 등재했다. `/sample`에 `moderation`
    섹션(4항목, 기본·로딩·오류 + BlockedUsersList는 빈 상태까지) 등록. Task 042B(관리자
    콘솔)가 쓸 `reports.status` 전이 계약(`pending`→`resolved`|`dismissed`, service-role
    경로로만 가능)을 문서에 정리해 인계했다. **같은 20일차 후속 — DESIGN 교차검증(major
    0건)이 "AC1 원문이 명시한 게시판·채팅 어느 쪽도 배선 안 됨"을 지적했고, 팀장이 "동시
    작업 충돌 우려가 소멸했다"고 판정해 그 자리에서 배선을 지시**했다: `board-view-models.ts`
    (`isAuthorBlocked` 추가)·`BoardListContainer`·`PostDetailContainer`·`BoardListItem`(카드
    전체를 접음 — `<Link>` 안에 `<button>` 중첩을 피하려고 접힘 상태에선 `<Link>` 자체를
    DOM에서 뺀다)·`PostDetail`(본문만 접음). 채팅은 `MessageListContainer`→`MessageRoomContainer`
    (배열→`Set`)→`MessageList`→`MessageBubble`(`isSenderBlocked`)로 배선했고, 실시간 메시지도
    초기 메시지와 같은 렌더 경로를 타므로 구조적으로 동일하게 접힌다(코드 경로 분석 — 실
    브라우저 소켓 검증은 못 함, 정직하게 남김). `/sample` board·chat 섹션에 데모 4항목 추가.
    `npx tsc --noEmit`·`npm run lint` clean. I-072를 "해결됨"으로 갱신(세션 중 차단 갱신
    미반영 등 경미한 잔여만 남김). 상세: `docs/decisions/report-block-042a.md`(§9).
- **Task 042B: 신고·차단·관리자 콘솔 (FR-080~FR-082) — 관리자 콘솔** (원 Task 042)
  - 팀·리뷰: A팀 / 리뷰 DESIGN(B팀)
  - 공수: 5.8인일 (M)
  - 기간: 원 배정 주차는 `docs/ROADMAP/team/02.DESIGN.md` 소관(이 문서의 읽기 범위 밖이라
    옮겨 적지 않는다) — **팀장 재배정**으로 실제 착수·완료는 21일차(2026-07-25)다
  - 선행 대기: Task 042A(A팀 자신, 완료) · Task 038(BOARD, 완료)
  - **재배정 사유(팀장 지시, 21일차)**: 원래 DESIGN 배정이었으나 이번 회차에 CREW로
    옮겨졌다. (a) CREW가 20일차에 042A(신고·차단)를 직접 구현했고
    `docs/decisions/report-block-042a.md` §6에 042B가 소비할 `reports.status` 전이 계약
    (service-role 경로 전제)을 CREW 본인이 정리해 인계했다 — 그 계약을 SQL 동작과 대조해
    실측 확인한 사람도 BOARD였지만, 계약을 세운 사람은 CREW라 문맥을 이미 갖고 있다.
    (b) DESIGN은 같은 회차에 크리티컬 패스 위의 Task 036(통합 테스트)에 전념해야 해서
    비크리티컬 경로인 042B를 옮길 여지가 있었다(이 사람의 담당 근거 "다른 사람이 막혔을 때
    예비 작업을 흡수할 여지가 가장 큰 자리"와 정확히 부합).
  - 참조: FR-082, D-008·D-014, NFR-015, **D-049**(system_admin 식별)·**D-050**(신고 처리
    RPC·감사 로그 어휘·소프트삭제 연동), `docs/decisions/report-block-042a.md`(§6 인계 계약)
  - 의존: Task 042A(CREW 자신, 21일차 인계 계약 소비) · Task 038(BOARD, `audit_logs` 스키마)
  - 상태: 완료 (21일차, 2026-07-25) — 029A §7.5·029B §8이 "새 결정 필요"로 이월한
    `system_admin` 식별을 `profiles.is_system_admin boolean` 컬럼 + 자가 승격 차단
    트리거로 확정했다(**D-049**). 신고 처리(FR-082 AC1 "삭제/기각/계정 제재")는
    `admin_list_reports`·`admin_resolve_report` SECURITY DEFINER RPC 2개(029B 2단 구조)로
    구현했다 — 042A 인계 문서가 제안한 "service_role 경로"는 팀장 지시("RLS/SECURITY
    DEFINER로 강제하라, 042A의 FR-081 AC2 수준을 요구한다")에 따라 채택하지 않고, RPC
    내부에서 `is_system_admin`을 직접 재확인하는 방식을 택했다(**D-050**). `remove_content`는
    기존 `posts`/`comments`/`chat_messages`의 `deleted_at`을, `suspend_account`는 028부터
    이미 있던 `profiles.status='suspended'`를 재사용해 새 스키마 없이 완결했다. `audit_logs.
    action`에 `report.*` 5종 추가(`report.dismissed`·`report.post_removed`·`report.comment_
    removed`·`report.chat_message_removed`·`report.account_suspended`) — RPC가 단일
    트랜잭션으로 직접 INSERT한다(I-054 회피 원칙의 연장). 마이그레이션 5건 적용(신규
    컬럼·RPC 2종 + 실측 중 발견한 트리거 충돌·컬럼명 모호성 수정 2건 + 결정 번호 정정 1건).
    실측(전부 `begin`…`rollback`) 18개 시나리오 — 비관리자·anon 거부, 관리자 정상 처리
    3종(기각·콘텐츠 삭제·계정 제재), 이미 처리된 신고 재처리 거부, 존재하지 않는
    대상·이미 삭제된 대상·이미 제재된 계정 등 경계 조건 전부 PASS. 자가 승격 차단도
    실측(트리거 예외 + RLS 행 0건 재확인 — 최초 시도에서 "예외 없음"만 보고 성공으로
    오판할 뻔한 것을 행 값 재조회로 교정, 방법론 정직하게 기록). `get_advisors(security)`
    신규 WARN 0건. 앱 레이어: `lib/data/supabase/admin.ts`(mock 없음, Task 032 이후 전례)·
    `lib/rules/report-resolution.ts`(순수 함수, 대상 유형별 가능 액션 판정)·
    `lib/actions/resolve-report.ts`·`components/admin/`(신규 도메인 — `AdminReportQueue`·
    `AdminReportQueueSkeleton`·`AdminReportsContainer`)·`(app)/admin/{layout,page}.tsx`
    (AC2 — 비관리자는 `notFound()`). 이 세션 사용자 본인 계정(`0625chopin@gmail.com`)을
    최초 관리자로 지정해 실 로그인으로 `/admin` 검증이 가능하게 했다. `/sample`에 `admin`
    섹션 등록(4상태, 오류 패널은 실제 세션으로 제출해도 재현되는 도메인 오류 포함).
    `npx tsc --noEmit`·`npm run lint`는 이 Task가 만든/고친 파일 전부 clean(전체 저장소
    기준으로는 BOARD의 진행 중인 Task 041 파일에서 무관한 사전 실패 다수 존재 — 손대지
    않음). **미검증**: 브라우저 실클릭(팀장 전용 운영 규칙), `report.post_removed` 감사
    로그(코드 대칭 구조상 안전할 것으로 추정하나 미실측, I-076). 신규 이슈 4건 등재
    (I-075~I-078 — 관리자 지정 UI 부재·`report.post_removed` 미실측·처리 이력 화면 부재·
    콘텐츠 삭제+계정 제재 복합 처리 불가). 상세: `docs/decisions/admin-console-042b.md`.

---

## 주차별 내 일정

**연속된 유휴 주는 한 줄로 묶는다.** 유휴 주에는 사유와 대체 작업을 함께 적는다.

| 주차 | 기간 | 진행 Task | 배정 인일 | 여유 |
| --- | --- | --- | ---: | ---: |
| 1주차 | 2026-07-27 ~ 2026-08-02 | 004 | 1.8 | 2.2 |
| 2주차 | 2026-08-03 ~ 2026-08-09 | 005 | 2.8 | 1.2 |
| 3주차 | 2026-08-10 ~ 2026-08-16 | 009B | 4.0 | 0.0 |
| 4주차 | 2026-08-17 ~ 2026-08-23 | 009B → 008 | 3.0 | 1.0 |
| 5주차 | 2026-08-24 ~ 2026-08-30 | 010 | 3.0 | 1.0 |
| 6주차 | 2026-08-31 ~ 2026-09-06 | **유휴** — Phase 3 전량이 Task 013(DESIGN)을 기다린다 | 0.0 | 4.0 |
| 7주차 | 2026-09-07 ~ 2026-09-13 | 015B | 4.0 | 0.0 |
| 8주차 | 2026-09-14 ~ 2026-09-20 | 015B → 016B | 4.0 | 0.0 |
| 9주차 | 2026-09-21 ~ 2026-09-27 | 016B | 4.0 | 0.0 |
| 10주차 | 2026-09-28 ~ 2026-10-04 | 016B → 016A | 4.0 | 0.0 |
| 11주차 | 2026-10-05 ~ 2026-10-11 | 016A | 4.0 | 0.0 |
| 12주차 | 2026-10-12 ~ 2026-10-18 | 016A → 017A | 4.0 | 0.0 |
| 13주차 | 2026-10-19 ~ 2026-10-25 | 017A | 4.0 | 0.0 |
| 14주차 | 2026-10-26 ~ 2026-11-01 | 017B | 4.0 | 0.0 |
| 15주차 | 2026-11-02 ~ 2026-11-08 | 017B | 2.0 | 2.0 |
| 16~28주차 | 2026-11-09 ~ 2027-02-07 | **유휴** — Task 024(DESIGN) 이후 `025 → 026 → 027 → 028 → 029A → 029B`가 CORE 단독 직렬 사슬이다. **13주 연속 유휴이며 이 일정의 최대 유휴 구간이다** | 0.0 | 52.0 |
| 29주차 | 2027-02-08 ~ 2027-02-14 | 030 | 4.0 | 0.0 |
| 30주차 | 2027-02-15 ~ 2027-02-21 | 030 | 3.5 | 0.5 |
| 31주차 | 2027-02-22 ~ 2027-02-28 | 039 | 4.0 | 0.0 |
| 32주차 | 2027-03-01 ~ 2027-03-07 | 039 | 4.0 | 0.0 |
| 33주차 | 2027-03-08 ~ 2027-03-14 | **유휴** — Task 040·042A가 Task 032(DESIGN)를 기다린다 | 0.0 | 4.0 |
| 34주차 | 2027-03-15 ~ 2027-03-21 | 040 | 4.0 | 0.0 |
| 35주차 | 2027-03-22 ~ 2027-03-28 | 040 | 4.0 | 0.0 |
| 36주차 | 2027-03-29 ~ 2027-04-04 | 040 → 042A | 4.0 | 0.0 |
| 37주차 | 2027-04-05 ~ 2027-04-11 | 042A | 4.0 | 0.0 |
| 38~47주차 | 2027-04-12 ~ 2027-06-20 | **유휴** — `034 → 036 → 037 → 043A → 043B`가 직렬이고 잔여 Task가 없다 | 0.0 | 40.0 |

**가용 인일 4.0을 넘는 주가 없다.** 유휴는 총 **107.9인일**이며 전체 가용(47주 × 4.0 = 188.0)의 **57.4%**다. 16~28주차의 13주 연속 유휴는 [`SCHEDULE.md` 「병렬 폭이 팀 수보다 작은 구간과 유휴 처리」](../../SCHEDULE/SCHEDULE.md#병렬-폭이-팀-수보다-작은-구간과-유휴-처리)의 대체 작업으로 채운다 — 특히 **I-016 SMTP 공급자 후보 비교**가 이 사람의 다음 Task(030)를 여는 열쇠이므로 최우선이다.

---

## 나를 막는 것 · 내가 막는 것

| 방향 | 상대 | Task | 기한 |
| --- | --- | --- | --- |
| 내가 기다린다 | CORE | Task 001 → 나의 005 | 2026-08-02 |
| 내가 기다린다 | DESIGN | Task 002 → 나의 009B | 2026-08-02 |
| 내가 기다린다 | CORE | Task 006 → 나의 008·009B | 2026-08-09 |
| 내가 기다린다 | BOARD | Task 009A → 나의 010 | 2026-08-23 |
| 내가 기다린다 | DESIGN | Task 013 → 나의 015B·016A·016B·017A·017B | 2026-09-06 |
| 내가 기다린다 | CORE | Task 029B → 나의 030 | 2027-02-07 |
| 내가 기다린다 | DESIGN | Task 035 → 나의 040 | 2027-01-17 |
| 내가 기다린다 | DESIGN | Task 032 → 나의 040·042A | 2027-03-14 |
| 나를 기다린다 | DESIGN | 나의 004 → Task 013 | 2026-08-02 |
| 나를 기다린다 | DESIGN | 나의 005 → Task 011 | 2026-08-09 |
| 나를 기다린다 | CORE | 나의 008 → Task 020A | 2026-08-23 |
| 나를 기다린다 | DESIGN | 나의 009B → Task 021A | 2026-08-23 |
| 나를 기다린다 | BOARD | 나의 030 → Task 038 | 2027-02-21 |
| 나를 기다린다 | DESIGN | 나의 030 → Task 036 | 2027-02-21 |
| 나를 기다린다(해소) | DESIGN | 나의 042A → Task 042B — **21일차 팀장 재배정으로 042B도 CREW 담당이 되어 내부 선행으로 전환**(위 담당 Task 042B 항목 참고) | 2027-04-11 |

**내가 4주차에 Task 004를 늦추면 DESIGN의 Task 013이 밀리고, 그러면 Phase 3 전량이 밀린다.** Task 004는 1.8인일짜리 결정 Task지만 **크리티컬 패스의 바로 옆에 붙어 있다** — 1주차 안에 끝낸다.

---

## 리뷰 담당

| Task | 담당자 | 리뷰 주차 |
| --- | --- | --- |
| 003 | BOARD(B팀) | 1주차 |
| 011 | DESIGN(B팀) | 3주차 |
| 014 | BOARD(B팀) | 5주차 |
| 013 | DESIGN(B팀) | 6주차 |
| 018B | BOARD(B팀) | 10주차 |
| 021B | DESIGN(B팀) | 12주차 |
| 023 | BOARD(B팀) | 16주차 |
| 024 | DESIGN(B팀) | 18주차 |
| 031 | DESIGN(B팀) | 30주차 |
| 033 | BOARD(B팀) | 36주차 |
| 034 | BOARD(B팀) | 39주차 |
| 042B | DESIGN(B팀) | 39주차 |
| 045 | DESIGN(B팀) | 43주차 |
| 043A | BOARD(B팀) | 45주차 |

---

## 시작 불가 구간

| 막힌 Task | 이슈 | 해소 기한(역산) | 그동안 할 것 |
| --- | --- | --- | --- |
| Task 030 | **I-016** 커스텀 SMTP 공급자 미선정 | **2027-01-25** (착수 29주차의 2주 전) | 16~28주차 유휴 구간에 **Resend·SendGrid·Amazon SES 후보를 비용·발송 한도·도메인 인증 절차 기준으로 비교**하고 권고안을 낸다. **SPF/DKIM 인증에 리드타임이 있어** 공급자 확정만으로는 부족하므로 발송 도메인 준비도 함께 진행한다. 확정되면 D-\*로 승격하고 PRD §8 "도입 예정" 구획에 등재한다(**D-036**) |

**이 사람이 지는 `🚧` Task는 1건뿐이다.** 규칙 5에 따라 나머지 1건(Task 033, **I-017**·**I-018**)은 B팀 BOARD이 진다 — **한 사람이 차단 Task를 2건 이상 지면 하나가 막힐 때 나머지도 함께 멈춘다.**

**I-016이 기한을 넘기면** Task 030이 밀리고, Task 030은 Task 036(통합 테스트)의 4개 선행 중 하나이므로 **v0.2 게이트가 같은 폭으로 밀린다.** Task 039도 030 뒤에 있어 함께 밀린다.
