# 29일차 작업 로그 (2026-07-30)

## 회차 요약

- 활성 팀원: **4명 전원**(CORE · DESIGN · CREW · BOARD).
- 이번 회차 배치 근거: **v0.1 로드맵 Task 001~045는 24일차에 전량 완료됐다.** 25일차부터 다섯
  회차 연속으로 배치가 잔여 이슈로 구성됐고, 이번 회차는 **D-080 릴리스 종료 게이트가 우선순위를
  정했다** — (A) 릴리스 차단 1건(I-141)이 최우선, (C) 외부 입력 대기 5건이 두 번째 조건,
  그리고 28일차 워크로그가 명시한 다음 회차 개시 항목(크루명 위반 정리 → DB CHECK, I-135/I-139
  귀속 분리)이 나머지였다.
- 결과: 로드맵 Task 완료 **0건**(잔여 0건이라 정상). 기존 이슈 **2건 해소**(I-141 · I-040),
  신규 이슈 **2건 등재**(I-142 · I-143), 신규 결정 **1건**(D-086). 마이그레이션 **2건 신규 적용**.
  회차 중 발견·해소가 같이 끝나 등재하지 않은 이슈 **2건**(아래 별도 절). 전체 테스트 4종
  **2회 통과**(교차검증 수정 전후).
- **이번 회차의 성격**: **게이트의 첫 조건이 회차 종료 시점에 처음으로 충족됐다.** 28일차에는
  (A)가 같은 회차 안에서 1→0→1을 왕복했는데, 이번엔 I-141을 닫고 신규 2건이 모두 (B)로
  분류돼 **종료 시점 재확인에서도 (A)가 0건이다.** 다만 게이트는 여전히 열려 있다 — 두 번째
  조건인 (C) 전건 결정이 4건 미충족이고, 남은 4건 전부가 팀의 손을 벗어난 외부 입력이다.
- **동시에 이번 회차는 "(C)를 줄이는 세 번째 방법"을 실증했다.** I-040은 사용자가 값을
  불러준 것도(경로 1), 팀이 현행값 확정안을 올린 것도(경로 2, D-083) 아니라 — **원문이
  기다린다고 적은 조건이 이미 충족돼 있는지 확인해서** 닫혔다. 그 조건(반려 이력 화면)은
  **6회차 전에 이미 만들어져 있었다.**

## 사용자 결정 2건 (회차 시작 시)

1. **I-040에 D-083 방식을 적용한다** — 팀이 현행 구현 기준 확정안을 올리고 팀장이 승인.
   28일차 트리아지 문서가 "이 건은 6건과 달리 팀 제안-팀장 승인 경로로 옮기지 않는다"고
   판단했던 것을 뒤집은 결정이다. 결과: **D-086으로 닫혔다.**
2. **I-038은 (C) 유지 — 손대지 않는다.** 금칙어 사전을 팀이 발명하면 안 된다는 28일차 CREW
   판단을 그대로 유지했다. 게이트 두 번째 조건은 그만큼 미충족으로 남는다.

## 팀원별 완료 내역

### CORE (01.CORE.md)

- 완료 Task: **없음**(잔여 로드맵 Task 0건). 배정 1건.
- 해소 이슈: **I-141**((A) 릴리스 차단 유일 항목)
- 산출물:
  - 마이그레이션 1건 — `i141_withdraw_join_request_atomic_rpc`(`20260730033748`)
  - 신규 — `docs/decisions/i141-atomic-withdraw-join-request.md`
  - 수정 — `src/lib/data/supabase/join-request.ts` · `database.types.ts`
- **설계**: I-054에서 이미 검증된 029B 2단 구조(`private.*` DEFINER + `public.*` INVOKER 래퍼)를
  그대로 재사용해 두 UPDATE를 단일 트랜잭션에 넣었다. **새로 설계하지 않은 것이 옳았다** —
  같은 결함 클래스에 같은 해법을 쓰면 검증 비용도 재사용된다.
- **원자성을 구조로 보장했다.** 함수에 `EXCEPTION` 핸들러를 **아예 두지 않았고**, 그 이유를
  함수 주석에 직접 적었다 — "plpgsql `BEGIN..EXCEPTION`은 savepoint를 만들어 핸들러가 잡고
  계속 진행하면 그 앞의 쓰기가 살아남는다(28일차 팀장 검증 논거)". **28일차에 팀장이 CORE의
  코드를 읽어 확립한 논거가 29일차에 CORE의 설계 근거로 되돌아왔다.**
- 자기반증 4항목: 정상 회귀 · 강제 실패 시 부분 커밋 0건(`count(*)`) · 가드 트리거의
  `requested→rejected` self-service 전이가 non-nested(`pg_trigger_depth()=1`) 경로에서 성립 ·
  RLS narrowing이 실제로 직접 UPDATE를 막음(0행). 전부 신규 테스트 크루로 수행 후 rollback.
- **RLS 정책 정리의 근거를 D-064/D-065 분기로 설명했다** — 정당 경로가 "클라이언트 직접 UPDATE"에서
  "서버 RPC 단독"으로 바뀌었으므로 신청자 본인 UPDATE 분기를 제거했다. 정책 수는 57→57
  (drop + 동명 재생성이라 불변).
- **스스로 남긴 한계**: 정책 이름(`join_requests_update_requester_or_staff`)을 유지했는데
  narrowing 후에는 실질 staff-only라 **이름과 동작이 어긋난다**(I-085 관례에 따라 이름 유지,
  문서화로 대응). 동시 접속 레이스(진짜 병렬 HTTP)는 이번에도 미재현 — I-054와 같은 신뢰 수준.

### CREW (03.CREW.md)

- 완료 Task: **없음**(잔여 로드맵 Task 0건). 배정 1건 + 교차검증 2건.
- 산출물:
  - 마이그레이션 1건 — `product_value_check_constraints_083`(`20260730033654`)
  - 신규 — `docs/decisions/product-value-check-constraints-29.md`
  - 수정 — `docs/issue-triage-release-readiness.md` §4-A · `docs/ISSUES.md`(I-067 블록)
- **D-083 확정값 4종을 DB CHECK로 승격했다** — 핸들(3~20자 + `^[a-z][a-z0-9_]*$`) · bio 150 ·
  크루명 30 · 크루 소개 300. 앱 레이어에만 있던 제약이 DB에 박혔다(public CHECK 35→39).
- 음성 7건 · 양성 4건 양쪽 실측. 경계값(정확히 30자·300자·3자·20자·150자)을 포함했다.
- **"삭제하지 말 것" 메모를 그냥 지나치지 않았다.** 삭제 대상 크루의 `description`에 20일차
  DESIGN이 남긴 `"archived 크루 브라우저 실측 전용 — 삭제하지 말고 그대로 둘 것"`이 있었다.
  진행 전에 조사해 28일차 D-083이 이 crew id를 명시적으로 선행조건으로 지목한 것을 근거로
  삭제를 진행하고, **판단 근거를 문서 §0에 남겼다.** 삭제 전 전체 덤프도 남겼다.
- **팀장 교차검증에서 2건 정정**(아래 별도 절) — 그중 하나는 CREW의 결론을 뒤집지 않았지만
  **비용 보고를 정직하게 만들었다.**
- 교차검증 2건 수행: BOARD의 I-040 제안서(4축 PASS, **Task 017A 발견**) · DESIGN 산출
  (2축 PASS · 1축 FAIL · 1축 경미). **두 건 다 팀장이 놓친 것을 찾았다.**

### DESIGN (02.DESIGN.md)

- 완료 Task: **없음**(잔여 로드맵 Task 0건). 배정 2건 + 교차검증 수정 2건.
- 산출물:
  - 신규 — `docs/design/i032-tone-check-29/`(README.md + png 10장)
  - 수정 — `docs/ISSUES.md`(I-135 · I-139 블록)
- **I-135/I-139 귀속을 확정했다 — 28일차에 미확정으로 남았던 항목이다.** 사전 합의된 구분
  절차(".next를 먼저 지우지 말고 재시작만 시도")를 그대로 실행했다: `sample/page.tsx`를 5회
  저장해 증상 재현 → **`.next` 삭제 없이 프로세스 재시작만으로 해소** → 파일 원복으로 재현 →
  다시 재시작만으로 해소. **같은 세션에서 2회 연속 재시작 단독 해소 → I-135로 확정.**
- **28일차 실패를 반복하지 않았다** — 관찰 카운트를 고칠 때 **상태 줄과 본문을 함께** 고쳤고,
  I-139의 "5회 관찰" 집계에 이번 관찰을 넣지 않은 이유를 명시했다. 28일차에는 한 이슈의 관찰
  대장을 다른 이슈에 옮겨 적어 상태 줄("1회")과 본문("6회")이 어긋났다.
- **부수 발견이 절차를 정밀하게 만들었다** — 재시작 직후의 일시적 `ChunkLoadError`(500)가
  I-139의 증상 2번과 **겉보기가 같은데 재시도 한 번으로 자연 해소**됐다. 같은 에러 메시지가
  최소 두 원인(재컴파일 레이스 / 캐시 오염)에서 나온다는 뜻이라, "`.next` 삭제 전에 한 번 더
  재요청" 단계를 완화 절차에 넣자고 양쪽 블록에 제안했다.
- I-032 톤 확인: 라이트·다크 양쪽 이상 없음, 조정 제안 없음. 대비 재실측은 하지 않았다
  (28일차에 DESIGN 실측과 팀장 독립 재계산이 소수점까지 일치해 확정됨).
- **dev 서버를 두 번 정리했다**(프로세스 0 · 포트 3000 해제) — 팀장 전체 테스트의 신뢰성이
  여기 달려 있다(I-129). 임시 편집한 `ToastTriggerPreview.tsx`도 원복 확인(`git diff` 0).

### BOARD (04.BOARD.md)

- 완료 Task: **없음**(잔여 로드맵 Task 0건). 배정 1건 + 교차검증 1건 + draft 등재 2건.
- 해소 이슈: **I-040**(팀장 승인 → D-086)
- 산출물:
  - 신규 — `docs/decisions/join-request-withdrawal-diagram-i040.md`
  - draft — `docs/ISSUES.draft.BOARD.md`(2건) · `docs/DECISIONS.draft.BOARD.md`(1건),
    **팀장이 마감에 번호 부여 후 삭제**
  - 수정 — `docs/ISSUES.md`(I-040 블록 참조 줄)
- **§③이 이 제안서의 가장 강한 부분이다** — `crew-membership-transition.ts` docstring이 **이미
  정확한 라벨**("오너/임원 반려 · 본인 자진 철회")을 갖고 있고, 같은 docstring이
  "`requirements.md`는 다음 회차에 고친다"고 명시한 것을 찾아냈다. 즉 I-040은 "제품 결정 대기"가
  아니라 **단일 소스가 23~24일차에 이미 확정한 값의 파생 문서 반영이 6회차째 밀린 것**이었다.
  요구사항 2.4절 자신이 그 모듈을 단일 소스로 선언하고 있어(`requirements.md:167`) 논거가
  문서로도 뒷받침된다.
- 교차검증 1건 수행: CREW의 CHECK 승격 작업(3축 PASS + 규칙 공백 1건 → I-142).
  **로컬 마이그레이션 `.sql`과 `pg_get_constraintdef` 실제 정의를 직접 대조**한 것이 정확했다
  (I-051 — 도구가 로컬 파일을 만들지 않으므로 손으로 쓴 파일이 어긋날 수 있다).
- **사실과 달라진 문서를 스스로 찾아 팀장에게 넘겼다** — 트리아지 §4-C가 "I-040 이번 회차 손대지
  않음, (C) 유지"로 남아 있는 것이 자기 제안서 제출로 사실과 달라졌음을 지적하고, **CREW 결함이
  아니라 팀장 마감 몫**이라고 정확히 귀속했다.

## 교차검증 결과

**이번 회차는 교차검증이 양방향으로 돌았다** — 26·27일차에는 루프가 돌지 않아 팀장이 전건을
직접 수행했고, 28일차에는 CREW가 일방향으로 두 명을 검증했다. 이번엔 **CREW ↔ BOARD가 서로를
검증했고 CREW → DESIGN도 돌았다.**

- **팀장 → CORE(I-141)**: **PASS — 8축 독립 재현.** CORE 보고를 기다리지 않고 DB·코드에서 직접
  확인했다: 2단 구조(`prosecdef`) · **`EXCEPTION` 핸들러 0개**(함수 본문 직독) · 가드 트리거의
  자기 전이 분기가 `auth.uid()` 기준이라 DEFINER 경로에서도 매치되고 `requested→rejected`가
  허용 목록에 명시 · EXECUTE 권한이 I-054 선례와 **형상 동일**(`anon` 제외) · RLS 정책의 신청자
  직접 UPDATE 분기 제거 확인 · **구 코드의 `.eq("status","requested")` 가드가 RPC에 그대로
  보존돼 동작 변화 없음**(`git diff`) · 호출부의 신규 `forbidden` 코드가 기존 `!ok` 분기로 흡수 ·
  Mock 시그니처 유지(NFR-034). 마이그레이션 버전 집합 SHA-256도 CORE 보고값과 **정확히 일치**
  (`414c2f0b7b42b02946bb31f313f7f83b2ab4e49ead3b26ca66e78d3eb86a4d08`, 로컬·원격 122건).
  - **팀장이 미리 의심한 축이 하나 기각됐다**: 두 번째 UPDATE의 `and cm.status='requested'`가
    0행 매치 시 조용히 성공하는 것을 결함으로 의심했으나, **구 코드에도 같은 가드가 있었으므로
    이번 변경이 만든 문제가 아니다.** 그 가드는 오히려 필요하다 — 없으면 `active` 멤버십을
    `rejected`로 덮을 수 있다.
- **팀장 → CREW(CHECK 승격)**: **FAIL 2건**(아래 이슈 절). 회차 시작 전에 잡아 둔 기준선
  (위반 행 1건 · 캐스케이드 5행 · 2단 종속 0건)과 대조해 삭제 범위가 정확함을 확인했다.
- **BOARD → CREW**: **3축 PASS + 규칙 공백 1건.** 로컬 마이그레이션 파일 vs DB 실제 정의 대조,
  트리아지 §4-A 갱신 정확성(§2·§6 잔여 서술 없음), **범위 임의 확대 없음**(I-038 금칙어가
  마이그레이션에 유입되지 않았음을 `pg_constraint` 전수 조회로 확인 — 사용자 결정 위반 0건).
- **CREW → BOARD**: **4축 PASS.** mermaid 붙여넣기 블록을 `requirements.md:184-200`과 **줄 단위로
  대조**해 의도된 1줄 외 16줄이 문자 그대로 일치함을 확인(무관한 줄 조용히 변조 0건). 행 번호
  참조 정확. 다이어그램 판독(상태 노드는 도달 경로를 기억하지 않으므로 나가는 엣지가 도달 경로와
  무관하게 적용된다) 성립. **그리고 4축에서 이 회차 최고의 발견을 했다**(아래 별도 절).
- **CREW → DESIGN**: **2축 PASS · 1축 FAIL · 1축 경미.**
  - **축 1이 DESIGN의 판정을 더 강하게 만들었다.** 팀장이 "재시작이 그 자체로 `.next` 캐시를
    무효화하면 구분 기준이 무너진다"고 교락을 의심했는데, CREW가
    `node_modules/next/dist/docs/.../turbopackFileSystemCache.md`를 찾아 **Turbopack이 `.next`
    캐시를 빌드 사이에 저장·복원하며 v16.1.0부터 dev 기본 활성화**(이 프로젝트 16.2.11)임을
    확인했다. 재시작은 캐시를 지우지 않고 **복원**하므로, 캐시가 오염됐다면 재시작으로 안 풀리고
    프로세스 내부 상태 문제는 풀린다 — **교락 없음이 문서로 확인되고 구분 절차의 타당성이 올라갔다.**
  - **축 3 FAIL** — Dialog가 Toast를 대표하지 못한다(아래 이슈 절).
  - 축 4 경미 — png 개수·README 설명 불일치.

## 발견·해결한 이슈

1. **[CREW] "어떤 미결 이슈도 이 행의 생존에 의존하지 않는다"가 사실과 어긋남** → 팀장이 삭제 후
   상태를 직접 셌더니 **DB에 archived 크루가 0건**이었고, I-067의 남은 범위가 정확히
   "`CrewHomeContainer`가 archived 크루를 열람 전용으로 렌더하는지 미검증"이다. 지운 행이
   **그 유일한 픽스처**였고 20일차 메모의 문구(`"archived 크루 브라우저 실측 전용"`)가 그 용도를
   말하고 있었다. **삭제 판단 자체는 유지**(D-083이 명시적으로 요구, `private.disband_crew`로
   재생산 가능) — 문제는 **비용을 0으로 보고한 것**이었다.
   → 해소: §0에 정정 블록 추가(문서화된 의존성은 없으나 실측상 픽스처가 사라진다), 재생산 절차
   기록(신규 크루 → `disband_crew`, 보호 크루 `729ced18-…`를 이 목적으로 쓰지 말 것),
   `ISSUES.md` I-067 블록에도 같은 안내(이슈만 보는 사람도 알 수 있게).
   → **부수 확인**: I-067 블록에 이미 "`CrewHomeContainer.tsx`에 `archived` 전용 분기 자체가
   없다"는 기록이 있어 픽스처 소실의 실제 타격은 처음 우려보다 작다.
2. **[CREW] "앱-DB 문자 그대로 일치"가 부정확함** → DB는 `char_length(btrim(x))`, 앱은
   `x.trim().length`인데 **두 함수의 공백 집합이 다르다.** 팀장 실측: `btrim(E'\t x \t') = 'x'`가
   **false**(탭·NBSP 미제거). `28자+탭3`은 DB에서 31자로 거부되고 JS에서는 28자로 통과한다.
   → **그래도 실제로는 어긋나지 않는다** — 쓰기 경로 3곳(`create-crew.ts:70-71` ·
   `update-crew-info.ts:65-66` · `update-account-profile.ts:46`)이 전부 저장 전 JS `.trim()`을
   하므로 `btrim`이 무연산이 되고, 앱을 우회하는 경로에서는 DB가 더 엄격한(안전한) 방향으로 갈린다.
   → 해소: §5를 **조건부 등가**로 재작성하고 그 불변식("모든 쓰기 경로가 저장 전 JS trim을 한다")을
   명문화. 새 쓰기 경로가 trim을 빠뜨리면 친절한 검증 메시지 대신 raw `23514`가 노출된다는 것
   (D-030 ③ 대상)까지 남겼다. **요약 문장도 함께 정정**했다 — 본문만 고치고 요약을 남기면 다음
   사람이 요약만 읽고 오독한다(28일차 I-135 실패 유형).
3. **[BOARD] 기각한 대안이 I-040 원문이 제안한 대안과 다름** → 제안서는 `withdrawn` **상태**
   신설을 기각했는데, 원문(`ISSUES.md:560-562`)이 제안한 것은 `CrewMembershipEvent`에
   `withdraw_request` **이벤트**만 추가(상태는 `rejected` 유지)하는 안이었다. 비용 구조가 다르다
   (이벤트 안은 DB·판정 무변경). **원문에 적힌 안이므로 근거 없이 배제하면 반드시 다시 꺼내진다** —
   28일차에 BOARD 자신이 세운 원칙이다.
   → 해소: 대안을 A(상태)·B(이벤트)로 분리해 B를 별도 판정. **판정 방식이 옳았다** — 결론을
   내리기 전에 **소비자 수와 실행 경로 경유 여부를 실제로 셌다**: `CrewMembershipEvent` 소비자가
   정의 파일 밖에 **0곳**(망라성 오류 없음, 타입 추가는 싸다)이지만 **철회 함수 두 곳이
   `TRANSITIONS`를 애초에 경유하지 않아**(Mock은 373행에서 직접 대입, 실데이터는 import조차 없음)
   이벤트를 더해도 **죽은 타입이 된다.** 영구 기각이 아니라 **보류 + 재검토 조건**(두 함수를
   경유로 재작성 = I-143)으로 남겼다. 팀장이 `grep`으로 독립 재현 — 호출부는 `mock/crew.ts`
   227·250·290·310행과 `seed/generate-crews.ts` 4곳이고 373행은 그 목록에 없다.
4. **[BOARD] 제안서 §3이 실제보다 약하게(미래형으로) 서술됨** → CREW가 교차검증에서 **Task 017A가
   이미 I-040의 실질 우려를 해소해 뒀음**을 찾았다(아래 별도 절). 제안서는 이를 "확인하지 못한
   것"에 두고 있었다.
   → 해소: "확인된 사실 — Task 017A 화면이 이미 이 근사가 무해함을 증명한다" 절을 신설해
   4개 파일:라인 + `git log` 근거를 인용하고, 이를 **권고 1의 가장 직접적인 정당화**로 세웠다.
5. **[DESIGN] Dialog가 Toast를 대표하지 못함(커버리지 공백)** → CREW가 코드로 대조: **Drawer는
   Dialog와 구조적으로 동일**(둘 다 `bg-black/10`+`backdrop-blur-xs`, `bg-popover ring-1
   ring-foreground/10`)해 생략이 정당하지만, **Toast는 오버레이 자체가 없다**(`ToastViewport`에
   백드롭 없음) — DESIGN이 명명한 "오버레이 합성 패턴" 카테고리에 애초에 속하지 않는다.
   → **이것을 draft 이슈로 미루지 않고 같은 회차에 닫은 것이 중요한 판단이다.** "배경 토큰이
   같으니 위험 낮음"은 **토큰 값 층위**의 논증이고 미확인으로 남은 것은 **렌더 인상 층위**였다 —
   **28일차에 I-032를 (A) 릴리스 차단으로 만든 논증과 정확히 같은 형태**다. `toast.tsx`도 28일차
   알파 상향 대상 11개 파일 중 하나였다.
   → 해소: DESIGN이 Toast 라이트·다크 스크린샷 1쌍을 추가하고(둘 다 링이 뚜렷, 문제 없음)
   README 분류를 **"비오버레이 부유 표면"**으로 분리했다.
6. **[DESIGN] png 개수·README 설명 불일치(경미)** → 9장인데 표는 8장만 참조하고
   `dark-dialog-close.png`가 설명되지 않았다. → 확대본(`dark-dialog-close-zoom.png`)을 삭제하고
   라이트와 동일한 원본 뷰포트 스크린샷으로 통일. **png 10장 = README 참조 10건, 1:1 일치**
   (팀장 재확인).
   - **팀장 정정**: 배정 메시지에 "png 10장"이라 적은 것은 팀장의 오류였다 — `ls` 출력에서
     `README.md`를 png로 잘못 셌다. 실제 9장이 맞고 CREW 지적이 정확했다.

## CREW가 찾은 것 — I-040의 실질 우려는 6회차 전에 이미 해소돼 있었다

**이번 회차 교차검증의 최고 성과다.** I-040 원문(`ISSUES.md:558-559`)이 남긴 유일한 실질 우려는
이것이었다:

> 이 근사가 실제로 맞는지(예: 크루 설정의 "반려 이력" 화면이 **나중에 생기면** 철회와 반려를
> 섞어 보여주는 게 맞는지)는 고객 확인이 없었다.

**그 화면은 9일차에 이미 만들어졌고 처음부터 섞지 않았다.**

| 근거 | 내용 |
| --- | --- |
| `JoinRequestPanel.tsx:38-40` | docstring이 **"'처리 내역' 탭이 I-040을 해소한다"**고 명시 |
| `JoinRequestPanel.tsx:20-24` | `HISTORY_BADGE_VARIANT` — `rejected:"outline"` / `withdrawn:"secondary"` |
| `ko.ts:401,403` | `"반려됨"` / `"철회함"` 별개 문구 + I-040 참조 주석 |
| `crew-member-view-models.ts:41-43` | 타입 레벨에서 "I-040이 요구하는 대로 뭉개지 않는다" 선언 |
| `git log --diff-filter=A` / `git log -S'withdrawn: "secondary"'` | **둘 다 `8c0b3e1`**(2026-07-24, Task 017A 원 구현) — 나중에 덧붙인 것이 아니다 |

**왜 이게 큰가**: "사용자 확인이 필요하다"며 6회차를 버틴 근거가 **이미 충족돼 있었는데 아무도
재확인하지 않았다.** 이슈 원문이 조건을 **미래형**으로 적어 두면("~화면이 나중에 생기면"), 그
조건이 이미 충족됐는지 다시 확인하는 사람이 없어 (C)에 무기한 머문다. 트리아지 §1의 "(B) 판정을
재사용할 때 근거가 이번 회차에도 성립하는지 확인한다"는 조건을 **(C)에도 똑같이 적용해야
한다** — §4-C에 재사용 가능한 교훈으로 남겼다.

그리고 이 발견에 도달한 경로가 특기할 만하다. 팀장이 지적한 것은 "기각한 대안이 원문 안과
다르다"는 **절차적 공백 하나**였고, BOARD가 그것을 메우려 **실행 경로를 파는 과정에서** CREW가
화면 쪽 사실을 찾아냈다.

## 릴리스 종료 게이트 (D-080) — 첫 조건 충족

| 조건 | 상태 | 근거 |
| --- | --- | --- |
| (A) 릴리스 차단 0건 | **충족** | I-141 해소(CORE, 팀장 8축 독립 재현). 신규 2건은 둘 다 (B) |
| (C) 전건 결정 | **미충족 — 4건** | I-038 · I-055 · I-057 · I-094 |

- **(A)가 회차 종료 시점에 0인 것은 처음이다.** 28일차는 같은 회차 안에서 1→0→1을 왕복했다.
- **게이트는 여전히 열려 있다** — 종료 조건은 두 개의 AND다.
- **남은 4건이 팀의 손을 벗어나 있다는 점이 이제 유일한 병목이다.** I-038은 법무·운영 확인,
  나머지 3건은 **계정·과금·대시보드 접근 권한** 자체가 필요하다. **I-057은 여전히 우선순위가
  가장 높다** — 미확인이면 FR-003이 실사용자에게 조용히 깨져 있을 수 있다.
- **최종 분류: (A) 0건 · (B) 32건 · (C) 4건 = 36건**(직전 36건 − 해소 2 + 신규 2로 총계 불변).
- **회차 중 발견·해소가 같이 끝난 2건은 등재하지 않았다**(Toast 커버리지 공백 · png 불일치) —
  28일차 I-137·I-138·I-140과 같은 처리다. **그 판단의 근거는 "사소해서"가 아니라 "같은 회차에
  실측으로 닫혔기 때문"이다.**

## 회차 운영 — D-082 규칙 2회차 연속 충돌 0건

- **결정 번호 충돌 0건 · 이슈 번호 충돌 0건.** 4명 전원 소환 프롬프트에 D-082 규칙을 넣었고,
  팀원 3명이 draft에 번호 없이 썼다(BOARD 3건). 팀장이 마감에 D-086 · I-142 · I-143을 일괄
  부여하고 draft 3개 파일을 삭제했다(잔존 0 확인).
- **번호를 `grep`으로 검증했다** — `docs/prioritization-and-risks.md` 6행이 지시하는 대로
  `grep -nE "^### D-0"`로 실제 최댓값(D-085)을 확인했고 "다음 번호" 줄(D-086)과 일치했다.
  `I-*`도 같은 방식으로 확인(최댓값 I-141, 줄 값 I-142 — 일치).
- **팀원이 이미 번호가 있는 블록 안을 편집한 사례가 여러 건 있었고 전부 규칙에 부합한다** —
  CREW의 I-067 안내 추가, DESIGN의 I-135·I-139 관찰 갱신, BOARD의 I-040 참조 줄. **새 헤딩을
  붙인 팀원은 없다.**
- **기록 정확성 부기**: BOARD가 draft 두 번째 항목을 "이미 들어가 있었다"고 보고했으나, 팀장이
  요청 **전에** 실행한 `grep -n "^## "`에서는 헤딩이 1개(3행)뿐이었고 요청 후 2개(3행·46행)가
  됐다. 결과물은 정확하므로 고칠 것은 없다 — **회차 기록은 기억이 아니라 관측으로 남긴다**는
  원칙에 따라 팀장 관측대로 적는다.

## 마이그레이션 로컬-원격 대조 (28일차 채택 표준)

CORE가 작업 전 120건 해시 일치를 확인했고, 작업 중 CREW가 동시에 1건을 적용해 일시적으로 로컬
누락이 생겼다가 CORE가 자기 파일을 만든 뒤 재대조했다.

**팀장 독립 재현 결과: 완전 일치.**

| 항목 | 값 |
| --- | --- |
| 원격 `schema_migrations` | **122건** |
| 로컬 `supabase/migrations/*.sql` | **122건** |
| 버전 집합 SHA-256(양쪽) | `414c2f0b7b42b02946bb31f313f7f83b2ab4e49ead3b26ca66e78d3eb86a4d08` — **일치** |

개수만 세면 서로 다른 파일이 우연히 같은 개수일 수 있으므로 **버전 집합 자체를 해시로** 대조했다.
CORE가 보고한 해시와 팀장이 독립 계산한 해시가 문자까지 같다.

## 팀장 전체 테스트 (항상 실행 — 이번 회차는 2회)

**dev 서버 정지 상태에서 실행했다**(I-129 오염원 제거 — DESIGN이 두 번 정리하고 프로세스 0개·
포트 3000 해제를 확인해 보고했고, 팀장이 매번 재확인했다).

| 명령 | 1회차(교차검증 수정 전) | 2회차(DESIGN 수정 후) |
| --- | --- | --- |
| `npm run lint` | **통과** — 0 problems | **통과** |
| `npx tsc --noEmit` | **통과** — 출력 0줄 | **통과** |
| `npm test` | **통과** — 4 files / **34 tests** | **통과** — 34 tests |
| `npm run build` | **통과** — 전 라우트 생성 | **통과** |

**`tsc` 통과가 진짜인지 검증했다**(27일차에 확립한 절차) — `src/lib/__tsc_probe.ts`에 타입 오류를
주입해 **`TS2322`가 정확히 보고되는 것**을 확인하고 즉시 삭제했다(워킹트리 잔존 0 확인).

## DB 델타 (회차 시작 → 종료, 팀장 측정)

| 항목 | 시작 | 종료 | 설명 |
| --- | ---: | ---: | --- |
| public 테이블 | 27 | 27 | 변화 없음 |
| public 정책 | 57 | 57 | CORE의 narrowing은 drop + 동명 재생성이라 수는 불변 |
| public 함수 | 56 | **57** | +1 (`withdraw_join_request` 래퍼) |
| private 함수 | 26 | **27** | +1 (같은 RPC의 DEFINER 구현체) |
| **public CHECK 제약** | 35 | **39** | **+4** — D-083 확정값 승격(CREW) |
| 마이그레이션 | 120 | **122** | +2 신규 적용 |
| crews | 14 | **13** | −1 (크루명 위반 행 캐스케이드 정리) |
| archived crews | 0 | **0** | I-067 픽스처 소실 — 재생산 절차 기록됨 |
| profiles | 21 | 21 | 변화 없음 |
| 시스템 관리자 | 1 | 1 | 변화 없음 |

`get_advisors(security)` 신규 WARN **0건**.

**DB 잔존물 점검(팀장)**: 테스트/검증 크루는 **1건뿐**이고 그것이 보호 대상
`729ced18-2016-459a-94c3-e7959dfe808c`(Task 036, active)다 — CORE의 자기반증용 신규 크루가
rollback으로 남지 않았음이 독립 확인된다. **이 크루는 계속 archived로 만들지 말 것.**

## 문서 갱신

- `docs/ROADMAP/team/*.md` 상태 마커: **갱신 없음**(완료 로드맵 Task 0건)
- `docs/team/*.md`: 변경 없음
- `docs/prioritization-and-risks.md`: **D-086 등재**, D-085에 29일차 후속 한 줄 추가,
  "다음 결정 번호" → **D-087**
- `docs/ISSUES.md`: I-141·I-040 해결됨, **I-142·I-143 등재**, I-067 픽스처 안내,
  I-135(2회 관찰)·I-139(귀속 제외 명시) 갱신, "다음 이슈 번호" → **I-144**
- `docs/requirements/requirements.md`: **2.4절 다이어그램 엣지 라벨 확장 + 29일차 각주(D-086)** —
  BOARD가 승인 전이라 손대지 않은 부분을 승인자인 팀장이 적용했다
- `docs/issue-triage-release-readiness.md`: 29일차 갱신 — §2 집계((A) 0 · (B) 32 · (C) 4) ·
  §3 I-141 해소 · §4 헤더 · **§4-C 전면 재작성**(BOARD 지적) · §5 신규 2건 근거 + 등재 안 한
  2건 근거 · **§6 게이트 첫 조건 충족**
- 신규 — `docs/decisions/i141-atomic-withdraw-join-request.md` ·
  `docs/decisions/product-value-check-constraints-29.md` ·
  `docs/decisions/join-request-withdrawal-diagram-i040.md` · `docs/design/i032-tone-check-29/`
- draft 파일: **0건**(D-082 절차대로 병합 후 삭제 확인)

## 다음 회차에 열리는 Task

**로드맵 Task는 없다 — v0.1 001~045는 전량 완료됐다.**

1. **게이트의 남은 조건은 (C) 4건뿐이고 전부 팀 밖에 있다.** I-038(법무·운영) · I-055(Sentry
   계정·DSN) · I-057(Supabase 대시보드 이메일 템플릿) · I-094(요금제). **I-057이 최우선** —
   미확인이면 FR-003이 실사용자에게 조용히 깨져 있을 수 있다. 사용자에게 다시 물어볼 시점을
   팀장이 정해야 한다.
2. **(C) 각 항목에 "조건이 이미 충족됐는지" 점검을 먼저 적용한다** — I-040이 그렇게 닫혔다.
   다만 남은 4건은 조건이 "외부 계정·법무 확인"이라 조용히 충족될 성질이 아니어서 실제로 줄어들
   여지는 I-040보다 작다.
3. **I-143 + D-086 대안 B는 함께 집는다.** 두 철회 함수(`mock/crew.ts:362-375` ·
   `supabase/join-request.ts`)를 `TRANSITIONS` 경유로 재작성하는 것이 대안 B(`withdraw_request`
   이벤트) 재검토의 선행조건이다. 지금 이벤트만 추가하면 죽은 타입이 된다.
4. **I-142** — "데이터 안 운영 메모 vs 이후 공식 결정" 충돌 판정 기준을 팀 규칙으로 만들지
   결정한다. BOARD가 규칙 후보를 draft에 적어 뒀고 그 내용이 I-142 본문에 병합돼 있다.
5. **I-067 픽스처 재생산** — archived 크루가 0건이라 `CrewHomeContainer` 렌더 검증에 픽스처가
   필요하다. 신규 크루 → `private.disband_crew`. **`729ced18-…`를 이 목적으로 쓰지 말 것.**
   다만 I-067 블록에 "`CrewHomeContainer.tsx`에 archived 전용 분기 자체가 없다"는 기록이 이미
   있어 실제 타격은 작다.
6. **CORE가 남긴 두 가지** — ① `join_requests_update_requester_or_staff` 정책 이름이 narrowing
   후 실질 staff-only라 **이름과 동작이 어긋난다**(I-085 관례로 이름 유지, 문서화만 됨).
   ② 동시 접속 레이스(진짜 병렬 HTTP 2개)는 I-054·I-141 둘 다 미재현 — 22일차 BOARD가 확립한
   "실계정 JWT + 병렬 실 HTTP" 방식으로 언젠가 확인할 후보.
7. **I-135/I-139 후속** — 귀속은 갈렸지만 **트리거 조건(왜 file watcher가 놓치는지)은 여전히
   미확정**이다. DESIGN이 제안한 "`.next` 삭제 전 재요청 1회" 단계가 두 이슈 블록에 들어갔다.
8. **Drawer 포커스 링은 실측이 아니라 코드 대조로만 확인됐다**(README에 "실측 아님" 명시) —
   Toast처럼 저비용으로 채울 수 있는 잔여 공백이다.

## git

- 브랜치: `day-29`(`day-28`에서 분기)
- 커밋: `day-29` 브랜치의 단일 커밋(해시는 amend로 바뀌므로 적지 않는다 — 브랜치 팁이 단일 소스다)
- draft 파일: **0건**(병합 후 삭제 확인)
- 푸시: 사용자 확인 후 진행
