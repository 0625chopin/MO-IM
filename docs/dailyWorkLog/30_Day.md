# 30일차 작업 로그 (2026-07-30)

## 회차 요약

- 활성 팀원: **4명 전원**(CORE · DESIGN · CREW · BOARD).
- 이번 회차 배치 근거: **v0.1 로드맵 Task 001~045는 24일차에 전량 완료됐다.** 25일차부터 여섯
  회차 연속으로 배치가 잔여 이슈로 구성됐고, 이번 회차는 **29일차 워크로그의 "다음 회차에
  열리는 Task" 8항목이 우선순위를 정했다** — (C) 최우선 1건(I-057) 실측, I-143 + D-086 대안 B,
  I-067 픽스처 재생산, Drawer 실측, I-142 규칙화, 그리고 (B) 중 **순수 문서 문제 4건을 묶어**
  실제로 닫는 것.
- 결과: 로드맵 Task 완료 **0건**(잔여 0건이라 정상). 기존 이슈 **7건 해소**(I-019 · I-023 ·
  I-031 · I-050 · I-067 · I-142 · I-143), 신규 이슈 **1건**(I-144), 신규 결정 **1건**(D-087).
  마이그레이션 신규 **0건**(스키마 변경 없는 첫 회차). 전체 테스트 4종 **통과**.
- **이번 회차의 성격 — (B)를 실제로 줄인 첫 회차다.** 25일차부터 다섯 회차 동안 (B)는
  30~32건에서 사실상 움직이지 않았고(해소와 신규가 상쇄) 29일차 총계는 정확히 불변이었다.
  이번엔 **6건이 닫히고 1건만 열려 (B) 32 → 27건, 총계 36 → 31건(순감 5건)**이다.
- **(A)는 2회차 연속 0건이다.** 신규 I-144는 호출부가 0개인 죽은 코드라 실사용자 영향이
  정의상 0이다. **게이트는 여전히 열려 있다** — (C) 4건이 미충족이고 전부 팀 밖에 있다.

## 사용자 결정 3건

1. **I-057에 실제 메일 1회 발송을 승인**(회차 시작) — MCP로 대시보드 설정을 볼 수 없으므로
   수신 링크가 유일한 판정 근거라는 팀장 제안을 받아들였다.
2. **`CLAUDE.md` D-030 ③ 수정을 유지**(회차 중반) — 아래 "팀장 절차 누락" 절 참고.
3. **I-057을 링크 없이 마감, (C) 유지**(마감 직전) — 두 차례 요청에도 링크 회신이 없어
   팀장이 마감 방식을 물었고 사용자가 (C) 유지를 선택했다.

## 팀원별 완료 내역

### CORE (01.CORE.md)

- 완료 Task: **없음**(잔여 로드맵 Task 0건). 배정 2건 + 교차검증 1건 + 지적 수정 2건.
- 산출물:
  - 신규 — `docs/decisions/i057-password-reset-template-verification.md`
  - 수정 — `docs/decisions/i141-atomic-withdraw-join-request.md`(§8 신설) · `docs/ISSUES.md`
    (I-141 블록)
- **I-057 — 절반을 확정했다.** `POST /auth/v1/recover` 1회 호출(HTTP 200, 본문 `{}`,
  2026-07-30T05:00:08Z), `get_logs(auth)`에서 `user_recovery_requested`·status 200 교차 확인.
  **판정 기준을 발송 전에 문서화한 것이 이 작업의 핵심**이다 — `token_hash`+`type=recovery`면
  PKCE 확정, `token`+`/auth/v1/verify` 호스트면 기본 템플릿 확정. 그래서 **링크만 확보되면
  재조사 없이 즉시 끝난다.**
- **I-141 마지막 미검증 축을 실증했다.** 22일차 BOARD가 확립한 "실계정 JWT + 병렬 실 HTTP"
  방식으로 같은 `join_request`에 `withdraw_join_request` RPC를 셸 `&`+`wait`로 동시 2회 호출 →
  **A=`not_found` / B=`ok:true`로 정확히 하나만 성공**, 부분 커밋 0건. I-054·I-141 둘 다
  "미재현"으로 남겨 뒀던 항목이 닫혔다.
- 픽스처를 신규로 만들고(보호 크루 `729ced18-…`를 쓰지 않음) 정리 후 **crews 13 · archived 0 ·
  profiles 21 복귀**를 확인했다.
- **자기 문서의 재현 가능성을 지적받고 고쳤다** — §8이 요약 표만 실어 "진짜 병렬"을 문서만으로
  검증할 수 없었다(DESIGN이 서버 로그로 우회 검증). §8.2 "원시 결과"를 신설해 실제 셸 명령과
  두 응답 raw JSON, 그리고 DESIGN이 확인한 서버측 타임스탬프(10ms 이내)까지 인용했다.
- `docs/ISSUES.md` I-141의 옛 문장("동시 접속 레이스는 재현하지 않았다")에 **취소선 + "30일차
  후속으로 해소" 표시**를 달았다(원문은 지우지 않음).
- 교차검증 1건 수행: BOARD 산출 6축 전부 PASS(아래).
- **세션 한도로 종료**(14:50 KST 리셋) — I-057 링크 판정만 미완이고 나머지는 전부 마쳤다.

### CREW (03.CREW.md)

- 완료 Task: **없음**(잔여 로드맵 Task 0건). 배정 2건 + draft 등재 1건.
- 해소 이슈: **I-143**(Mock 해결됨 · Supabase 확인 완료) · **I-067**(수정 완료, 재검증은
  DESIGN이 대행)
- 산출물:
  - 신규 — `docs/decisions/i143-withdraw-transitions-passthrough.md` ·
    `src/lib/rules/crew-membership-transition.test.ts` ·
    `docs/design/i067-crew-home-archived-30/`(README + png)
  - 수정 — `src/lib/data/mock/crew.ts` · `src/components/crews/CrewHomeContainer.tsx` ·
    `docs/ISSUES.md`(I-067 · I-143 · I-135 블록)
  - draft — `docs/ISSUES.draft.CREW.md`(1건, **팀장이 마감에 I-144 부여 후 삭제**)
- **I-143 — 대칭을 근거로 삼았다.** `withdrawPendingCrewMembership`의 직접 대입과 수동 가드를
  `transitionCrewMembershipStatus(status, "reject_request")` 경유로 바꿨다. `rejectCrewMembership`
  (같은 파일 위쪽)이 이미 그 형태였으므로 **새 패턴을 발명하지 않았다.**
- **Supabase 경로는 경유시키지 않기로 판정하고 이유를 남겼다** — RPC가 DB 안에서 조건부 UPDATE
  한 문장으로 끝나므로 TS가 미리 계산해도 SQL이 동시성 방어로 같은 조건을 다시 검사해야 한다.
  **억지로 배선하지 않은 것이 옳았다**(팀장 배정문이 허용한 선택이고, BOARD가 근거 성립을 확인).
- **D-086 대안 B는 "여전히 미충족"으로 명시 판정했다** — 기존 이벤트(`reject_request`)만
  재사용했으니 새 유니온을 소비할 코드가 생기지 않았고, 그 함수 자체가 죽은 코드다. **애매하게
  남기지 않은 것이 중요하다** — 다음 회차가 이 판정을 근거로 삼는다.
- **I-067 — 실제 결함을 찾았다.** `CrewHomeContainer`가 활성 멤버십 분기에서 `crew.status`를
  전혀 읽지 않아 해산된 크루 홈이 일반 크루 홈과 똑같이 렌더됐다. 원인 규명이 정확하다 —
  `(app)/crews/[crewId]/layout.tsx`가 이미 게시판·채팅·멤버 관리·설정 전체에
  `ArchivedCrewBanner`를 띄우는데 **크루 홈만 `(app)` 밖이라 빠져 있었다.** 같은 패턴을 적용해
  최소 범위로 고쳤고, 쓰기 차단은 **I-070 영역**이라고 D-039와 같은 결로 선을 그었다.
- 부수 발견을 draft로 올렸다(→ I-144): 이 Mock 함수가 Task 032부터 배럴에서 재노출되지 않는
  **죽은 코드**라는 것. **"이번 배정 범위 밖"이라고 스스로 경계를 그은 것이 옳다.**
- **한계를 정직하게 남겼다** — 자기 수정을 브라우저로 재검증하지 못했다(I-135 증상 + 공유 서버
  kill 금지 지침). 그 사실을 I-067 블록에 명시했고, **그 정직한 서술이 DESIGN의 재검증을
  불러 오탐을 잡는 계기가 됐다**(아래).
- **API 서버 오류로 1회 중단**됐고(작업 손실 없음, 팀장이 `git diff`로 확인 후 재개 지시),
  이후 **세션 한도로 종료**됐다.

### DESIGN (02.DESIGN.md)

- 완료 Task: **없음**(잔여 로드맵 Task 0건). 배정 1건 + 교차검증 2건 + **미완 재검증 대행 1건**.
- 산출물:
  - 신규 — `docs/design/i032-tone-check-29/{light,dark}-drawer-close.png` ·
    `docs/design/i067-crew-home-archived-30/i067-crew-home-active-no-regression-30.png`
  - 교체 — `docs/design/i067-crew-home-archived-30/i067-crew-home-archived-after-fix.png`
  - 수정 — `docs/design/i032-tone-check-29/README.md` ·
    `docs/design/i067-crew-home-archived-30/README.md` · `docs/ISSUES.md`(I-067 · I-135 블록)
- **Drawer 포커스 링을 실측해 29일차 공백을 닫았다.** 실제 키보드 `Tab` 2회로 `DrawerClose`에
  도달해 `document.activeElement`에 `focus-visible:ring-ring/70`이 포함된 것까지 확인한 뒤
  촬영했다 — **프로그램적 초기 포커스(popup 컨테이너)를 대상에서 제외한 판단이 정확하다.**
- **미확인 범위를 좁히기만 하고 넓히지 않았다** — Drawer 안의 트리거 버튼·스와이프 핸들·"참석"
  버튼은 여전히 미확인이라고 명시하고, `Button`이 아닌 요소는 이 실측이 커버하지 않는다는
  이유까지 남겼다.
- **이 회차 최고의 성과 — CREW의 오탐을 잡았다.** 팀장 지시로 dev 서버를 재시작하고 I-067
  수정을 재검증한 결과, 배너가 실제로 렌더되고 활성 크루에는 안 나타나는 것(회귀 없음)을
  확인했다. 그리고 **CREW의 `after-fix.png`가 `before-fix.png`와 MD5가 완전히 같았다** —
  파일명만 after이고 내용은 수정 전 화면이었다. 새로 촬영해 교체하고 **그 경위를 README에
  남겼다**(감추지 않았다).
- **I-135를 4번째 관찰로 확정 귀속시켰다** — `.next`를 지우지 않고 프로세스 재시작만으로 최신
  소스가 반영됐다(3.5초 `Ready`). CREW가 "확정 못한 채 열림"으로 남긴 3번째 관찰이 이로써
  I-135로 귀속됐다. 상태 줄도 "3회"→"4회"로 함께 갱신했다(28일차 실패 유형 재발 없음).
- 교차검증 2건 수행: CORE 산출 6축 PASS(서버 로그 독립 조회 포함) · CREW 산출 3축 PASS.

### BOARD (04.BOARD.md)

- 완료 Task: **없음**(잔여 로드맵 Task 0건). 배정 2건 + 교차검증 2건 + 지적 수정 2건.
- 해소 이슈: **I-019 · I-023 · I-031 · I-050**(문서 잔재 4건) · **I-142**(→ D-087)
- 산출물:
  - 신규 — `docs/decisions/i142-legacy-note-vs-later-decision-precedence.md`
  - draft — `docs/DECISIONS.draft.BOARD.md`(1건, **팀장이 마감에 D-087 부여 후 삭제**)
  - 수정 — `docs/ISSUES.md`(I-019 · I-023 · I-031 · I-050 · I-142 블록) · `CLAUDE.md` ·
    `docs/prd/PRD.md` · `docs/ROADMAP/ROADMAP.md` · `docs/prd/PRD-validation.md`
- **I-142 규칙안이 판정 가능한 형태다** — 조건 ①(메모 목적이 이미 문서 결과로 종결) ②(열린
  어떤 이슈도 그 데이터 생존에 의존한다고 명시하지 않음)가 `grep`·문서 대조로 실제 판정 가능한
  구체적 조건이고 애매어가 없다. **적용 범위를 데이터 안 메모로 좁힌 것이 특히 좋다** — 코드
  주석은 `git blame`이, 순수 문서는 SKILL.md §0-1이 이미 담당한다고 배제 근거를 댔다.
- **반사실 검증에서 자기 제안의 실효를 축소해 보고했다** — "규칙이 29일차에 있었어도 CREW의
  판단은 바뀌지 않는다"고 인정하고, 실효는 **조건 불충족 사례에서 "멈추고 보고"를 기본값으로
  만드는 것**이라고 정확히 구분했다. **규칙의 가치를 "이번 결과가 달라지는가"로 재지 않은 것이
  옳다.**
- **문서 잔재 4건을 실측 기반으로 닫았다** — I-031은 문서만 믿지 않고 `generate-polls.ts`에서
  실제값을 재계산했고, I-023은 `components.json`·`src/components/ui/` 25종을 실물 확인한 뒤
  PRD §8.2 → §8.1로 이관했다.
- **닫을 수 없는 것을 억지로 닫지 않았다** — I-031의 세 번째 위치(`03.CREW.md:52`)는 팀원 문서
  읽기 금지 범위라 재확인하지 않고 그 사실을 disclosure로 남겼다. **팀장 확인 결과 그 위치는
  §6.2 원문 인용부라 고치지 않는 것이 옳았다.**
- 교차검증 2건 수행: CORE 산출 6축 PASS · CREW 산출 6축 PASS(아래 — 그중 하나가 회차의 분기점).

## 교차검증 결과

**이번 회차는 교차검증이 실제 오류를 두 번 잡았다** — 그중 하나는 팀원 스스로도 알 수 없었던
것이고, 다른 하나는 팀장의 절차 누락이었다.

- **팀장 → DESIGN(Drawer 실측)**: **PASS — 4축.** png 12장 ↔ README 고유 참조 12건의 **양방향
  차집합이 모두 공집합**임을 확인(개수만 세면 서로 다른 파일이 우연히 같은 개수일 수 있다).
  `git diff --stat -- src/`에 DESIGN 관련 변경 0줄 — 컴포넌트 미편집 보고 정확. README가 미확인
  범위를 좁히기만 한 것도 확인.
- **DESIGN → CORE(I-057 · I-141)**: **PASS — 6축, 두 축을 서버 로그로 독립 재현.**
  - **발송이 1회인지 직접 셌다** — `get_logs(auth)`에서 `/recover` 요청이 **정확히 1건**(다른
    path 73건과 대조). 팀장 지시(Gmail SMTP 한도 보호) 준수가 로그로 입증됐다.
  - **"진짜 병렬"을 문서 밖에서 검증했다** — 문서 §8에 원문 인용이 없어 순차/병렬을 구분할 수
    없자, `get_logs(api)`에서 `POST /rest/v1/rpc/withdraw_join_request`(UA `curl/8.18.0`) 2건의
    수신 타임스탬프가 **10ms 이내**임을 확인했다. **이 지적이 CORE의 문서를 재현 가능하게 만든
    §8.2를 낳았다.**
  - 요청 축 밖의 자발적 확인도 했다 — `pg_policies`에서 narrowing된 정책의 `qual` 원문을 직접
    조회해 신청자 self-service 분기가 실제로 없음을 확인, `pg_proc`에서 029B 2단 구조 확인.
- **CORE → BOARD(I-142 · 문서 4건)**: **PASS — 6축.**
  - **`CLAUDE.md` 추가 문구가 사실인지 코드로 검증했다** — `contracts.ts` docstring이 `DataResult`
    요구를 "## 왜 모든 **쓰기**가 예외를 던지지 않는가" 절 아래 한정하고 읽기 함수는 언급조차
    없음을 확인. **데이터 경계 담당자가 이 축을 본 것이 정확한 배정이었다.**
  - **Poll 72를 독립 재계산했다** — 생성기·`fixtures.ts`·`seed/index.ts`를 추적해 신규 70(open 6 ·
    passed 59 · rejected 3 · invalid 2) + 기존 2 = 72, passed 60. BOARD 주장과 일치.
  - **팀원 문서 읽기 금지를 지키면서 검증했다** — 세 번째 위치는 읽지 않고 "미확인 disclosure가
    남아 있는지"만 확인했다.
- **BOARD → CREW(I-143 · I-067)**: **PASS — 6축. 그중 축 6이 이 회차의 분기점이다.**
  - **`before-fix.png`와 `after-fix.png`의 MD5를 대조해 완전히 동일함을 발견했다**
    (`cd353f34aca18c62ef73a57539b8e723`, 42,251바이트). "재검증 미완"이라는 CREW의 서술이 사실과
    정확히 일치함을 물증으로 확인한 것인데, **결과적으로 그 스크린샷이 근거로 쓸 수 없다는
    뜻이기도 했다.** 팀장이 이를 DESIGN에게 전달해 재촬영으로 이어졌다.
  - 죽은 코드 발견을 `grep`으로 독립 재현했다 — 4곳 전부가 정의 1곳 + 주석 인용 3곳, **실제
    import·호출 0건**. `src/lib/data/index.ts` export 목록에 `./mock/*`가 없음도 확인.
  - **I-135 관찰 카운트의 상태 줄 ↔ 본문 일치를 확인했다**(28일차에 이 어긋남이 실제로 났다).
    28일차 관찰이 "카운트에 넣지 않는다"고 스스로 밝혀 총계와 어긋나지 않음까지 확인.
  - `npm test` 37/37 재실행, `crew-membership-transition.test.ts` 단독 3/3도 확인.
- **DESIGN → CREW(재검증 대행)**: **오탐 1건 정정.** 위 "팀원별 완료 내역" DESIGN 절 참고.
- **DESIGN → CREW(두 번째 스크린샷 별도 확인)**: **함정 아님으로 판정.** 팀장이 "`i067-settings-
  archived-banner-check.png`도 같은 함정일 수 있다"고 지시했고, DESIGN이 `/settings`를 다시 열어
  배너와 편집 폼이 실제로 렌더되는 것을 `browser_find`로 확인했다. **재촬영본의 MD5가 기존 파일과
  또 같았지만 이번엔 staleness가 아니다** — 그 라우트를 렌더하는 `(app)/crews/[crewId]/layout.tsx`는
  이번 회차에 **CREW가 건드리지 않은 파일**이고(수정 대상은 `CrewHomeContainer.tsx`뿐), 안 바뀐
  코드가 두 시점에 같은 화면을 내는 것은 정상이다.
  - **판단 기준을 옳게 세웠다** — "해시가 같다"가 아니라 **"이 라우트가 이번에 바뀐 코드를
    거치는가"**로 판단했다. 앞선 오탐에서 해시 일치가 결정적 증거였기 때문에 같은 신호를 기계적으로
    적용할 유혹이 있었는데, **같은 증거가 다른 결론을 낳는 조건을 구분한 것이 정확하다.** 중복
    파일을 버리고 기존 파일을 유지한 판단도 맞다.
  - 근거와 절차를 README에 새 절로 남겨(99행 이하) 다음 사람이 같은 의심을 반복하지 않게 했다.

## 발견·해결한 이슈

1. **[CORE] I-141 문서 §8이 재현 불가능했다** → 요약 표만 있어 "진짜 병렬"을 문서만으로 검증할
   수 없었다(DESIGN이 서버 로그로 우회). **실측 문서의 값은 재현 가능성인데 그것이 없었다.**
   → 해소: §8.2 "원시 결과" 신설 — 실제 셸 명령(`&`+`wait`), 두 응답 raw JSON, DESIGN이 확인한
   서버측 타임스탬프(10ms 이내)를 인용. **교차검증자의 우회 검증이 문서 안 근거로 승격됐다.**
2. **[CORE] `docs/ISSUES.md` I-141의 옛 문장에 해소 표시가 없었다** → "동시 접속 레이스는
   재현하지 않았다"가 그대로 남아 스캔하는 독자에게 사실과 반대로 읽혔다. **29일차 워크로그가
   명시한 실패 유형("본문만 고치고 요약을 남기면 다음 사람이 요약만 읽고 오독한다")과 같은
   형태라 품질 제안으로 넘기지 않고 수정 대상으로 판정했다.**
   → 해소: 옛 문장에 취소선 + "30일차 후속으로 해소" 표시. **원문은 지우지 않았다**(이전 판단을
   찾을 수 있어야 한다는 ISSUES.md 기록 원칙).
3. **[BOARD] I-031 블록 문구가 산술적으로 자기모순이었다**(CORE 지적) → "가결 60건 신규 + 기존
   1건 = 60건"은 60+1≠60이라 읽는 사람이 멈춘다. → 해소: CORE의 독립 재계산 내역(신규 70 + 기존
   2 = 72, passed 60, 상태 다양성 12)으로 교체. **숫자를 바꾼 게 아니라 내역 표현만 정정했다.**
4. **[BOARD] I-031을 "해결됨"으로 닫는데 상태 줄에 3곳 중 1곳 미수정이 없었다**(팀장 지적) →
   본문에는 disclosure가 있었지만 **상태 줄만 읽는 사람은 3곳 전부 처리된 줄 안다.**
   → 해소: 상태 줄에 "3곳 중 2곳 정정 · 1곳은 §6.2 원문 인용부라 미수정" 명시. **서술도
   정확해졌다** — "미확인이라 못 고쳤다"가 아니라 "인용부라 고치지 않는다"가 사실이다(팀장이
   `03.CREW.md:52`를 직접 읽어 확인: 원문 인용 뒤에 4일차 정정 메모가 이미 붙어 있어 인용을
   고치면 원문 대조가 깨진다).
5. **[CREW] I-067 "after" 스크린샷이 실제로는 수정 전 렌더였다**(BOARD가 MD5로 발견 → DESIGN이
   재촬영) → 파일명만 after이고 내용은 before와 바이트 단위로 동일했다. dev 서버가 소스 변경을
   반영하지 않은 상태(I-135)에서 찍혔기 때문이고, **CREW도 알 수 없었던 문제다** — 그래서 CREW의
   "재검증 미완" 서술은 정직했다.
   → 해소: DESIGN이 서버 재시작 후 재촬영해 같은 파일명으로 교체(MD5 `d9fa35a3…`), 회귀 확인용
   스크린샷 추가, **경위를 README에 남겼다**(감추면 다음에 같은 함정을 다시 밟는다).
   → **이 건이 회차의 분기점이다.** 팀원의 정직한 "미완" 표기 → 교차검증자의 MD5 대조 →
   팀장의 전달 → 다른 팀원의 재검증으로 이어졌다. **어느 한 단계가 빠져도 잘못된 근거가 문서에
   남았다.**
6. **[팀장] `CLAUDE.md` 배정 시 원문이 요구한 사용자 승인 절차를 확인하지 않았다** → I-050 원문의
   "후속"이 **"`CLAUDE.md`는 프로젝트 지침 파일이라 사용자(팀장 상위) 판단으로 결정한다"**고
   명시했는데, 팀장이 이를 확인하지 않고 BOARD에 문구 수정을 배정했다. **팀원 결함이 아니라 팀장
   결함이다.**
   → 해소: 수정 후 팀장이 `contracts.ts` docstring과 I-050 원문(Task 031의 "RLS 0행은
   `not_found`와 구분하지 않는다" 결정)을 직접 대조해 **추가 문구가 사실임을 확인**하고 사용자에게
   유지 여부를 물어 **승인**을 받았다. I-050 블록에 경위와 재발 방지책("`CLAUDE.md`·`AGENTS.md`를
   건드리는 배정은 팀원을 띄우기 전에 사용자 승인을 받는다")을 남겼다.
7. **[팀장] draft 병합 중 자동 치환이 I-144 헤딩·상태 줄을 잘라냈다** → `sed '1,5d'`가 draft의
   헤딩(3행)까지 삭제해 블록이 헤딩 없이 병합됐다. → 해소: 확인 과정에서 발견해 헤딩·상태 줄을
   복구했다(구조 검증: 헤딩 → 상태 → 영역 → 제보 → 등재 경위). **병합 후 `grep`으로 구조를
   확인하는 절차가 이 실수를 잡았다.**

## 릴리스 종료 게이트 (D-080) — (A) 2회차 연속 0건, (C) 여전히 4건

| 조건 | 상태 | 근거 |
| --- | --- | --- |
| (A) 릴리스 차단 0건 | **충족 (2회차 연속)** | 신규 I-144는 호출부 0건인 죽은 코드 — 실사용자 영향이 정의상 0 |
| (C) 전건 결정 | **미충족 — 4건** | I-038 · I-055 · **I-057** · I-094 |

- **최종 분류: (A) 0건 · (B) 27건 · (C) 4건 = 31건**(직전 36건 − 해소 6 + 신규 1).
- **(B)가 실제로 줄어든 이유**: 닫힌 6건 중 4건이 **순수 문서 문제**였다. 조사 비용이 낮은데도
  "릴리스를 막지 않는다"는 이유로 계속 밀려 온 항목들이고, **성격별로 갈라 저비용 항목을 묶어 한
  배정으로 처리하면 실제로 줄어든다**는 것이 이번 회차의 관찰이다.
- **(C)에 대해 새로 알아낸 것 — "절반 확정"이라는 상태가 실재한다.** I-057은 이제 둘로 쪼개진다:
  GoTrue 접수·코드 링크 계약은 **확정**, 대시보드 템플릿 형태는 **미확정**. 즉 (C)는 "팀이
  아무것도 못 한다"가 아니라 **"팀이 할 수 있는 데까지 밀어 두면 외부 입력이 도착한 순간 즉시
  닫힌다"**는 상태로 만들 수 있다. 남은 3건에도 같은 처리가 가능한지가 다음 회차 검토 대상이다.
- **29일차의 "세 번째 경로"는 이 건에 적용되지 않았다** — I-040은 "원문이 기다린다고 적은 조건이
  이미 충족돼 있었다"로 닫혔지만, I-057이 기다리는 것은 **팀 밖의 계정 접근 권한**이라 조용히
  충족되지 않는다. 29일차 §6의 예상("줄어들 여지는 I-040보다 작다")이 그대로 확인됐다.

## 회차 운영 — D-082 규칙 3회차 연속 충돌 0건

- **결정 번호 충돌 0건 · 이슈 번호 충돌 0건.** 4명 전원 소환 프롬프트에 D-082 규칙을 넣었고,
  팀원 2명이 draft에 번호 없이 썼다(CREW 1건 · BOARD 1건). 팀장이 마감에 **I-144 · D-087**을
  부여하고 draft 2개 파일을 삭제했다(잔존 0 확인).
- **번호를 `grep`으로 검증했다** — `grep -nE "^### D-0"`의 실제 최댓값(D-086)과 "다음 번호"
  줄(D-087)이 일치, `I-*`도 최댓값 I-143 · 줄 값 I-144로 일치.
- **팀원이 기존 번호 블록 안을 편집한 사례가 여러 건이고 전부 규칙에 부합한다** — CORE의 I-141
  갱신, CREW의 I-067·I-143·I-135 갱신, DESIGN의 I-067·I-135 갱신, BOARD의 I-019·I-023·I-031·
  I-050·I-142 상태 갱신. **새 헤딩을 붙인 팀원은 없다.**
- **팀원 2명이 회차 중 사용 불가가 됐고 배치를 재구성했다** — CREW가 API 서버 오류로 1회 중단
  (팀장이 `git diff`로 산출물 보존을 확인하고 재개 지시), 이후 CORE·CREW가 **세션 한도**로 종료
  (14:50 KST 리셋). **남은 2명에게 리뷰 짝 관계를 유지한 채 재배정했다** — DESIGN(CREW의 리뷰
  짝)에게 브라우저 재검증 + 교차검증, BOARD(CREW의 리뷰 짝)에게 문서·논증 축 교차검증. **축을
  갈라 중복을 피한 것이 두 사람의 검증이 서로 보완되게 만들었다.**

## 마이그레이션 로컬-원격 대조 (28일차 채택 표준)

| 항목 | 값 |
| --- | --- |
| 원격 `schema_migrations` | **122건** |
| 로컬 `supabase/migrations/*.sql` | **122건** |
| 버전 집합 MD5(양쪽) | `355bf67a67bd202fa612977167efc2ee` — **일치** |

**이번 회차는 마이그레이션 신규 적용이 0건이다** — 회차 시작·종료 해시가 같다. 25일차 이후
처음으로 스키마를 건드리지 않은 회차이고, 배치가 전부 앱 코드·문서·검증이었기 때문이다.

## 팀장 전체 테스트 (항상 실행)

**dev 서버 정지 상태에서 실행했다**(I-129 오염원 제거). DESIGN이 정지를 보고한 뒤에도 **새 PID로
서버가 다시 떠 있는 것을 팀장이 발견해 직접 정지**하고(포트 3000 해제 확인) 테스트를 돌렸다 —
보고를 그대로 받지 않고 재확인한 것이 필요했다.

| 명령 | 결과 |
| --- | --- |
| `npm run lint` | **통과** — 0 problems |
| `npx tsc --noEmit` | **통과** — 출력 0줄 |
| `npm test` | **통과** — **5 files / 37 tests**(29일차 34 → +3, CREW 신규) |
| `npm run build` | **통과** — 전 라우트 생성 |

**`tsc` 통과가 진짜인지 검증했다**(27일차에 확립한 절차) — `src/lib/__tsc_probe.ts`에 타입 오류를
주입해 **`TS2322`가 정확히 보고되는 것**을 확인하고 즉시 삭제했다(워킹트리 잔존 0 확인).

**테스트 이후에 들어온 변경 1건을 기록한다** — DESIGN이 종료 직전에 `i067-settings-archived-banner-
check.png` 검증 결과를 README에 추가했고, 이는 **문서(`.md`)뿐이라 4종 테스트 결과에 영향이 없다**
(코드·설정 변경 0). 재실행하지 않은 이유를 남기는 것이 "테스트 후 아무것도 안 바뀌었다"고 암시하는
것보다 정확하다. 커밋 시점 워킹트리는 깨끗했고 그 변경도 같은 커밋에 포함됐다.

## DB 델타 (회차 시작 → 종료, 팀장 측정)

| 항목 | 시작 | 종료 | 설명 |
| --- | ---: | ---: | --- |
| public 테이블 | 27 | 27 | 변화 없음 |
| public 정책 | 57 | 57 | 변화 없음 |
| public 함수 | 57 | 57 | 변화 없음 |
| private 함수 | 27 | 27 | 변화 없음 |
| public CHECK 제약 | 39 | 39 | 변화 없음 |
| 마이그레이션 | 122 | 122 | **신규 0건** |
| crews | 13 | **14** | +1 — I-067 검증용 archived 픽스처(의도적으로 남김) |
| archived crews | 0 | **1** | `2724533e-9e02-4609-8ad3-88becec6fe24` |
| profiles | 21 | 21 | 변화 없음 |
| 시스템 관리자 | 1 | 1 | 변화 없음 |

**스키마 변경이 0건이라 `get_advisors(security)` 재확인은 불필요하다**(새 테이블·함수·정책이
없으므로 새 WARN이 생길 표면이 없다). CORE의 레이스 실증 픽스처는 전량 정리돼 crews 증가분은
I-067 픽스처 1건뿐이다.

**DB 잔존물 점검(팀장)**: 검증용 크루는 2건이고 둘 다 의도된 것이다 — `729ced18-…`(Task 036,
**active 유지 확인** — 이 크루는 계속 archived로 만들지 말 것) · `2724533e-…`(I-067 픽스처,
archived). **I-067 픽스처는 다음 회차를 위해 남긴다** — DESIGN이 삭제 여부를 팀장 판단으로
넘겼고, 재생산 비용(신규 크루 → `private.disband_crew`)보다 남기는 비용이 낮다.

## 문서 갱신

- `docs/ROADMAP/team/*.md` 상태 마커: **갱신 없음**(완료 로드맵 Task 0건)
- `docs/team/*.md`: 변경 없음
- `docs/prioritization-and-risks.md`: **D-087 등재**, "다음 결정 번호" → **D-088**
- `docs/ISSUES.md`: I-019 · I-023 · I-031 · I-050 · I-067 · I-142 · I-143 **해결됨**,
  **I-144 등재**, I-057 절반 확정 기록, I-135 4회 관찰, I-141 옛 문장 취소선 처리,
  I-050 사용자 승인 경위, "다음 이슈 번호" → **I-145**
- `CLAUDE.md`: D-030 ③에 범위 한정 괄호(**사용자 승인**)
- `docs/prd/PRD.md` · `docs/prd/PRD-validation.md` · `docs/ROADMAP/ROADMAP.md`: I-023 · I-031 정정
- `docs/issue-triage-release-readiness.md`: 30일차 갱신 — 헤더 기준 시점 · §2 집계((A) 0 ·
  (B) 27 · (C) 4 = 31) + **닫힌 6건 표** · §5 닫힌 항목 취소선 표시 + **I-144 등재** ·
  **§6 게이트 30일차 절 신설**("절반 확정" 개념 포함)
- 신규 — `docs/decisions/i057-password-reset-template-verification.md` ·
  `docs/decisions/i141-atomic-withdraw-join-request.md` §8 ·
  `docs/decisions/i143-withdraw-transitions-passthrough.md` ·
  `docs/decisions/i142-legacy-note-vs-later-decision-precedence.md` ·
  `docs/design/i067-crew-home-archived-30/` · `src/lib/rules/crew-membership-transition.test.ts`
- draft 파일: **0건**(D-082 절차대로 병합 후 삭제 확인)

## 다음 회차에 열리는 Task

**로드맵 Task는 없다 — v0.1 001~045는 전량 완료됐다.**

1. **I-057은 링크 한 줄이면 닫힌다.** 판정 기준·절차가 문서에 완성돼 있어 재조사가 필요 없다.
   사용자가 메일 링크를 확인할 수 있게 되면 **가장 싸게 닫히는 (C) 항목**이다. 토큰 만료로
   기존 링크를 못 쓰면 1회 재발송이 필요하다.
2. **남은 (C) 3건에도 "절반 확정"을 적용해 본다** — I-055(Sentry DSN)는 SDK 배선까지 미리 해 두면
   DSN만 꽂으면 되는 상태로 만들 수 있고, I-094(요금제)는 Realtime Reports 경로가 이미 문서화돼
   있다. **I-038(법무·운영)은 이 방법이 통하지 않는다** — 금칙어 사전을 팀이 발명하면 안 된다는
   28~29일차 판정이 유효하다.
3. **I-144 삭제 여부를 판단한다** — Mock `withdrawPendingCrewMembership`을 ① 완전히 삭제
   (CLAUDE.md 원칙과 정합) ② Mock 전용 시나리오의 소비자로 승격 중 하나로. **BOARD가 호출부
   0건을 독립 재현해 뒀으므로 판단 근거는 이미 있다.**
4. **D-086 대안 B는 여전히 보류다** — CREW가 "이번 재작성으로도 미충족"으로 명시 판정했다.
   재검토 조건은 "`TRANSITIONS`를 경유하는 **살아있는** 코드가 생기는 것"이고, I-144를 삭제하는
   방향으로 가면 이 조건은 **더 멀어진다**(경유 코드가 아예 사라진다). 두 판단을 함께 해야 한다.
5. **I-070** — archived 크루의 설정 폼이 편집 가능한 채로 열린다(DB CHECK가 막는 UPDATE를 앱이
   도메인 오류로 못 바꾼다). CREW가 I-067 실측 중 **사실로 확인**했고 경계를 그어 남겼다.
   D-030 ③ 대상이라 우선순위가 낮지 않다.
6. **I-135는 트리거 조건이 여전히 미확정이다** — 4회 관찰로 **재현 방법(연속 저장)과 해소 방법
   (프로세스 재시작 단독)은 확정**됐다. "왜 file watcher가 놓치는지"만 남았고, 실용적으로는
   "재시작만 먼저"를 계속 적용하면 된다.
7. **저장소 루트에 `crews-360.png`가 있다**(2026-07-24 생성, 이번 회차 산출물 아님) — 이전 회차의
   위치 오류 잔재로 보인다. 소유자를 확인해 옮기거나 지운다. **이번 회차엔 손대지 않았다**(다른
   회차 산출물을 임의 처분하지 않는다).

## git

- 브랜치: `day-30`(`day-29`에서 분기)
- 커밋: `day-30` 브랜치의 단일 커밋(해시는 amend로 바뀌므로 적지 않는다 — 브랜치 팁이 단일 소스다)
- draft 파일: **0건**(병합 후 삭제 확인)
- 푸시: 사용자 확인 후 진행
