# 수동 검증 절차 색인 (Manual Verification Index)

**이 문서는 회차마다 갱신되는 살아있는 색인이다 — 파일명에 회차 번호를 붙이지 않는다.** CI가
아직 없어서(R-002, `docs/prioritization-and-risks.md`) 이 프로젝트의 회귀 방어 상당수는
"사람이 문서의 존재를 기억하고 다시 돌리는 것"에 전적으로 걸려 있다. 문서가 늘어나는 속도보다
색인이 늦으면 회차가 쌓일수록 체크리스트는 늘고 실행률은 0으로 수렴한다 — 이 문서는 그 수렴을
막기 위한 것이다.

**이 문서는 내용을 복제하지 않는다.** 각 절차의 SQL·curl·판정 기준의 단일 소스는 원 문서다.
여기서는 **무엇을 보장하는지·언제 돌리는지·얼마나 걸리는지·CI로 옮길 수 있는지**만 요약하고
원 문서 경로(및 절)를 가리킨다. 건수·날짜 같은 갱신 값은 이 문서 안에서만 쓴다(다른 문서에
복제하지 않는다, CLAUDE.md 단일 소스 원칙).

---

## 0. 어떻게 찾았는가 (33일차 최초 작성)

`docs/design/**/*.md`(11개)·`docs/decisions/*.md`(56개) 전수 67개 파일을 스캔했다. 방법:
① 파일명·헤딩으로 "감사(audit)"·"검증(verification)" 부류 선별 ② 본문에서 `체크리스트`·
`재현 절차`·`확인 방법`·`판정 기준`·`curl`·`begin;`·`언제`·`도착 즉시` 등 신호어를 grep ③
신호어가 걸린 파일은 해당 절을 읽어 "**사람이 나중에 다시 실행해야 하는, 기대 출력이 있는
재현 가능한 절차**"인지 "**이미 종결된 1회성 조사 로그**"인지 판별. 신호어가 안 걸렸어도
파일명이 감사류인 것(`i032-tone-check-29` 등)은 직접 읽어 확인했다. 판별 기준은 "이 문서가
없어지면 다음에 같은 걸 다시 확인할 방법이 없어지는가"였다.

**결과**: 재사용 가능한 검증 절차 **19개 파일 · 20개 절차**(하나는 파일 안에 절차 2개),
해당 없음(종결된 조사 로그·순수 결정 기록) **48개 파일**, 판단이 갈릴 수 있어 불확실로 남긴
것 **3개 파일**(§4). 전수 목록·판별 근거는 이 문서 갱신 시 재확인할 때 처음부터 다시 훑는
대신, 아래 표에 없는 새 후보만 추가로 검토하면 된다(§5).

---

## 1. 릴리스 전 반드시 (짧고, 매 배포 전 실행)

이 3개는 코드 영역과 무관하게 **매 릴리스 전에 돌리는 것을 권장한다** — 전부 30분 이내로
끝나고, 이 프로젝트의 가장 기초적인 회귀(빌드 깨짐·권한 노출)를 잡는다.

| 절차 | 무엇을 보장하는가 | 소요 시간 | CI 이관 |
| --- | --- | --- | --- |
| `docs/decisions/build-deploy-verification.md` §7 | `npm run build`·`tsc --noEmit`·`npm run lint`가 통과하고 Turbopack·이미지 API 설정이 위반 없이 유지된다 | 5~10분 | 가능(순수 CLI 명령) |
| `docs/design/private-schema-usage-guard-33/README.md` §3 | Supabase 대시보드 "Exposed schemas"가 여전히 `private`를 빼놓고 있다(SQL 레벨 방어가 구조적으로 불가능하다는 게 33일차 결론이라 이게 유일한 방어선) | 5분 이내 | 가능(curl 스크립트화 가능, 단 대시보드 설정 자체를 보는 경로는 사람) |
| `docs/decisions/permission-baseline.md` §2 + `docs/decisions/permission-baseline-raw-27.md` | RLS·GRANT·트리거·SECURITY DEFINER 권한 표면 전체가 직전 스냅샷과 일치한다(쿼리 8종) | 15~30분 | 부분(쿼리 실행은 스크립트화 가능, diff 판단은 사람 — 문서가 "CI 연동은 범위 밖"이라고 명시) |

**이 셋 중 하나라도 실패하면 배포를 막아야 한다** — 나머지는 아래 §2의 조건부 절차다.

---

## 2. 조건부 (해당 코드·스키마를 만질 때만)

| 절차 | 무엇을 보장하는가 | 트리거 조건 | 소요 시간 | CI 이관 |
| --- | --- | --- | --- | --- |
| `docs/design/rls-regression-checklist-33/README.md` §1~§4 | `join_requests` 승인의 archived-크루 차단이 RLS 한 곳에서 계속 살아있다 | §4 명시: 관련 정책·`is_crew_active`·officer 트리거 수정 시, 분기별 정기 점검(권장) | 10~15분 | 가능 |
| `docs/decisions/insert-axis-audit-102-103.md` | INSERT/ALL RLS 정책이 self-service 컬럼값을 계속 제한한다 | 신규 INSERT 정책 추가 시(자매 문서 §7이 회귀 체크리스트로 지정) | 15~20분 | 부분(쿼리는 스크립트화 가능, 판정은 사람) |
| `docs/decisions/delete-truncate-axis-audit-111-112.md` §7 | DELETE 정책·TRUNCATE GRANT·FK CASCADE 우회가 새 테이블에서도 막혀 있다 | 신규 테이블 추가 시, self-service RLS 추가 시 | 20~40분 | 가능 |
| `docs/decisions/cron-foundation.md` §4 | pg_cron 잡이 최근 24시간 안에 조용히 실패하지 않았다 | cron 잡 이상 의심 시, 신규 배치 잡 추가 시(상시 모니터링에 가깝다) | 1분 이내 | 가능 |
| `docs/decisions/chat-retention-035.md` §7 | 채팅 파기 pg_cron 잡이 실패하지 않았다 | 상시(§6과 같은 계열, cron 잡 모니터링) | 1분 | 가능 |
| `docs/decisions/chat-retention-035.md` §8 | 파기 배치 쿼리가 실사용 데이터 볼륨에서도 타임아웃 없이 끝난다 | 실사용 데이터가 축적된 뒤(현재는 추정치 기반) | 도착 대기형 — 도착 후 즉시 EXPLAIN 재실행 | 부분 |
| `docs/decisions/c-half-confirmed-31.md` §1(Sentry) | Sentry DSN이 발급되는 즉시 `report-client-error.ts` 교체가 안전한지 판정 | Sentry DSN 도착 시 | 도착 대기 — 도착 후 즉시 판정(사전 확정된 판정표 적용) | 불가(외부 서비스 가입·설정) |
| `docs/decisions/c-half-confirmed-31.md` §2(Supabase 요금제) | 확정 요금제가 NFR-006 연결 상한·과금 위험을 만족하는지 판정 | Supabase 요금제 확정 시 | 도착 대기 — 도착 후 즉시 판정 | 불가(대시보드 확인) |
| `docs/decisions/i057-password-reset-template-verification.md` §1·§3 | 비밀번호 재설정 이메일 링크가 코드가 기대하는 PKCE 계약과 일치한다 | 실제 링크 수신 시(현재 대기 중 — 미확정) | 도착 후 1분 이내 | 불가(이메일함 육안 확인) |
| `docs/design/calendar-palette.md` §6 | 크루 12색 팔레트가 배경 대비 3:1·CVD 구분성을 유지한다 | `--background` 라이트/다크 토큰 변경 시 | 30분~1시간 | 부분(대비 계산은 스크립트화 가능, 최종 판단은 사람) |
| `docs/decisions/appshell-height-chain-visual-regression-118-122.md` | 19라우트×라이트/다크×3뷰포트에서 스크롤·가시성·가로스크롤 회귀가 없다 | AppShell/레이아웃 높이·overflow CSS 변경 시(문서에 명시 없음, 추정) | 30~60분 | 부분(Playwright 스크립트화 가능, 스크린샷 판단은 사람) |
| `docs/design/display-layer-audit-33/README.md` §6 | "SQL만 확인하고 화면은 확인 안 함" 패턴이 새로 늘지 않았다 | §6 명시: 매 작업 회차 초반 | 1~2시간(census) + 항목별 추가 | 부분(grep census는 자동, DOM 실측은 사람) |
| `docs/decisions/integration-test-036.md` §2.4·§9 | 정적 대조 실패 3축(DB 강제 여부·RLS 컬럼 제한·스키마 전수)이 다음 통합 테스트에서 재발하지 않는다 | §2.4 명시: 다음 통합 테스트의 표준 체크리스트 | 1~2시간 | 부분 |
| `docs/decisions/performance-043a.md` | 렌더링 성능(LCP/INP/CLS)이 목표치(INP p75 ≤200ms 등)를 유지한다 | 렌더링 전략 변경, 성능 회귀 의심 시(문서에 명시 없음, 추정) | 30~60분 | 불가(CDP 스로틀링·수동 계측) |
| `docs/decisions/performance-043b.md` §7 | 실시간 파이프라인(DB 트리거→소켓→화면) 지연이 허용 범위다 | §7 명시: "다음에 실시간 경로를 다시 의심할 일이 생기면" | 45~90분 | 불가(CDP 계측) |
| `docs/decisions/realtime-broadcast-033.md` 부록 | Realtime Authorization이 authorized/unauthorized/bogus 케이스를 정확히 가른다 | broadcast 트리거·Realtime RLS 변경 시(문서에 명시 없음, 추정) | 15~20분 | 불가(소켓 수신 확인) |
| `docs/decisions/auth-integration-030.md` §6 | 테스트 계정이 Admin REST + SQL 삽입 방식으로 재현 가능하다(`/signup` 폼으로는 안 됨) | 테스트 계정 추가 필요 시 | 10~15분 | 불가(서비스 롤 수동 작업) |

---

## 3. 요약 — 분류 결과

- **총 재사용 절차**: 19개 파일, 20개 절차(§1 3개 + §2 17개)
- **릴리스 전 필수 / 조건부**: 3 / 17
- **CI 이관**: 가능 6 · 부분 9 · 불가 5(대시보드 클릭·이메일함 확인·소켓 육안 관찰·CDP 수동
  계측처럼 구조적으로 사람만 할 수 있는 단계 포함) — "가능"으로 표시된 것도 **R-002(CI 없음)
  때문에 현재는 전부 수동 실행**이다. 이관 가능성과 실제 이관 여부는 별개다.
- **해당 없음(종결된 1회성 조사 로그·순수 결정 기록)**: 48개 파일 — 재실행 트리거가 없어 이
  색인에서 뺐다.

---

## 4. 불확실 — 판단이 갈릴 수 있어 명시적으로 남김

| 파일 | 왜 애매한가 |
| --- | --- |
| `docs/decisions/i141-atomic-withdraw-join-request.md` §8.2 | curl 병렬 레이스 재현 스크립트는 재사용 가능한 템플릿이지만, 문서 자체는 "결함 없음"으로 닫힌 결론이다. `withdraw_join_request` RPC를 다시 손댈 때 회귀 테스트로 재활용할 가치는 있다고 판단해 여기 남긴다 — 트리거 조건: 그 RPC 재수정 시 |
| `docs/decisions/join-request-withdrawal-diagram-i040.md` | "원자적 RPC 전환 완료 시 재검토"라는 조건부 확인이 있지만 1회성 후속 확인에 가깝다 |
| `docs/decisions/i142-legacy-note-vs-later-decision-precedence.md` | 조건 확인 체크리스트 형태이나 코드·스키마 검증이 아니라 문서 거버넌스 판단 기준(레거시 노트 삭제/보류 규칙)이라 이 색인이 찾는 "실물 검증 절차" 범주 밖으로 판단했다 |

---

## 5. 이 색인을 최신으로 유지하는 규칙 (제안 — 결정 승격 필요)

**규칙(제안)**: 새 수동 검증 절차 문서(재실행 트리거·기대 출력이 있는 체크리스트)를 만든
사람은 **같은 작업 안에서 이 문서에 표 한 줄을 추가한다** — 원 문서 경로, 한 줄 요약, 트리거
조건, 소요 시간, CI 이관 여부. 문서 갱신 없이 체크리스트만 늘리는 것은 §0이 우려하는
"실행률 0 수렴"을 그대로 재현한다.

이 규칙은 팀 운영 규칙이라 CORE 단독으로 확정하지 않는다 — 팀장 판단을 위해
`docs/DECISIONS.draft.CORE.md`에 번호 없이 제안해 뒀다.

**추가 규칙(제안, 33일차 팀장 관찰)**: **표를 고칠 때 도입문·요약절의 건수를 함께 갱신한다
— 같은 문서 안에서 같은 숫자가 두 곳(도입문 vs 표, 본문 vs 요약)에 따로 적혀 있으면 한쪽만
고치고 다른 쪽을 잊는 사고가 반드시 난다.** 33일차 하루에만 이 패턴으로 숫자가 어긋난 사례가
세 번 있었다(I-155 처분 문서의 "19개" vs 실제 23개, I-158 draft의 "15곳" vs 나열 19개, 그리고
이 색인 §1의 "4개" vs 표 3행) — **세 건 모두 판정 자체는 맞았고 숫자만 틀렸다.** 공통 원인은
조사 단계가 아니라 **문서에 옮겨 적는 단계에서 숫자를 손으로 다시 쓰며 어긋나는 것**이다.
그래서 같은 값을 여러 곳에 중복해서 적어야 하는 문서(도입문+표, 본문+요약절 등)를 쓸 때는
가능하면 한쪽이 다른 쪽을 "표를 보라"는 식으로 가리키게 하고, 부득이 중복해야 하면 **고칠 때
전수 grep으로 같은 숫자가 몇 곳에 더 있는지 확인한 뒤 전부 함께 고친다.**
